<!--
SPDX-FileCopyrightText: 2026 James L. Burns and The GoPMgr Contributors
SPDX-License-Identifier: GFDL-1.3-or-later
-->

# Beta release backlog

This is the release-readiness backlog for the current feature set. It is not a
feature wish list. New features should wait unless they fix security, data
loss, or a release blocker.

## Release exit criteria

- Every P0 item is complete.
- No known critical security, data-loss, first-run, or installer defect is
  open for an advertised platform.
- `make verify`, `make release-scope`, `make license-check`, and
  `make check-release` pass from the release commit.
- Release notes describe every remaining limitation without implying unverified
  platform support or trusted signing.
- Installation, upgrade, and data-preserving removal have current evidence for
  each advertised package format.

## P0: must complete before wider beta distribution

| Item | Status | Completion evidence |
| --- | --- | --- |
| Preserve legacy Budget values at the desktop boundary | Done — automated boundary and component evidence | Project metadata and Dashboard Budget totals now use canonical decimal strings at the Wails boundary; they do not expose floating-point values or `int64` minor units as JavaScript numbers. This also fixes an edit path where a stale nonzero minor-unit value could override a later Budget edit or clear. Go tests cover the full signed-`int64` decimal range, invalid/overflow rejection without a project mutation, repeated edits, clear-to-zero, and JSON keys; component tests prove exact large/negative display and the settings payload. Native Wails interaction remains unverified for this specific edit path. The legacy Budget rollup remains separate from Cost Control and does not approve any Phase 2 lifecycle work. |
| Preserve Portfolio Analytics money and currency semantics at the desktop boundary | Done — automated and isolated native darwin/arm64 evidence | Portfolio Analytics converts DuckDB's internal exact minor-unit aggregates to canonical decimal strings at the Wails boundary. It computes Remaining in Go with checked exact subtraction and returns an error rather than a rounded total on overflow. It also accepts only one canonical reporting currency among readable projects, returns that ISO code with a successful rollup, and refuses a mixed-currency rollup because GoPMgr has no FX model. The renderer only groups/display strings and uses the returned sign for deficit styling; it does not perform monetary arithmetic or receive minor units. Tests cover the signed-`int64` range, negative Remaining, subtraction overflow, JSON types, values beyond JavaScript's safe integer range, a homogeneous DuckDB rollup, mixed-currency refusal, legacy/default currency canonicalization, and the visible UI error state. On 2026-08-21, the guarded launcher proved a fresh XDG root is used while the normal and legacy roots are sandbox-denied and unchanged; it retains the root for re-entry. In that retained profile, the native UI created an empty EUR project beside the populated USD project and showed the explicit no-FX refusal without a stale rollup. The launcher still cannot establish that no unrelated macOS/WebKit cache or preference is touched. This preserves the existing separation from Cost Control and does not approve Phase 2 lifecycle work. |
| Prevent silent editor data loss | In progress — scoped to darwin/arm64, unsigned local build | A shared guard protects every editor that registers a dirty draft — `WorkItemEditor`, `SprintList`, and `StakeholderManager` — across three exit paths: in-app navigation, project close, and native window close. A failed save or an edit made while a save is in flight stays visible and blocks the user from losing it; each editor disables its own timed auto-save (a successful modal save already closes the draft) and requires a second successful save before it will let a late edit through. `StakeholderManager` additionally preserves money values in exact minor units while they remain safe JavaScript integers, and fails closed rather than silently rounding an unsafe number. The native-close guard, added 2026-08-15, freezes renderer interaction once the user picks clean/saved/discarded, then Go grants and consumes a one-shot permit before invoking Wails' quit — covered by `main_test.go` and `frontend/src/lib/native-close.test.ts`. On 2026-08-18, two Go concurrency tests, run under `go test -race`, proved the native-close guard's mutex-protected permit state machine is race-free under real concurrent access (the existing tests had only ever run it sequentially); a fault-seeded lock removal confirmed the tests are load-bearing by reproducing three real data races that the fix removes. A second test found a genuine TOCTOU window where a concurrent `EnableNativeCloseGuard` call could revoke a permit `CompleteNativeClose` had just granted — traced against the frontend's actual control flow and confirmed unreachable from the shipped UI, so no code change was needed there. A doc comment in `main.go` records two facts traced against the vendored Wails v2.13.0 source: every quit trigger (window-close on all platforms, Cmd+Q, the app's own Quit menu item) funnels through the same dispatcher into `beforeClose`, and `OnBeforeClose` runs asynchronously on darwin/linux but synchronously on Windows. `ProjectSettings.svelte` did not originally participate in this guard correctly. Live GUI testing on 2026-08-18 surfaced two real bugs: an unbreakable "Save and continue" loop, caused by `UpdateProjectIndustry` always returning a fresh server-stamped `updated_at` that `save()` folded into `draft`, which the autosave snapshot comparison could never converge on — fixed by excluding `updated_at` from the registered snapshot; and a more severe silent-data-loss bug where `save()` unconditionally overwrote `draft` with the server response on every save, discarding any edit typed while a save was in flight (the other three editors already guarded against this via their own `rebaseEditableChanges` helpers). Both fixes were verified by reverting each independently and confirming its regression test fails, then restoring it; the full frontend suite (253 tests) and `svelte-check` pass. The four editors' near-duplicate `rebaseEditableChanges` implementations were later consolidated into a shared `frontend/src/lib/rebase-editable-changes.ts` helper that blocklists backend-owned fields rather than allowlisting editable ones, so a newly added field is protected by default. Deriving each blocklist mechanically (interface fields minus the old function's named fields) caught one real gap — `ProjectMeta.budget_minor_units` was missing from an earlier hand-written version. Fault-seeded by adding a real field to `ProjectSettings`'s blocklist and confirming the regression test catches it. GUI verification was done by installing packaged `.pkg` builds under an isolated data directory and driving the app directly, starting 2026-08-16 and continuing across several sessions: window-close on `StakeholderManager` and `WorkItemEditor` (dialog fires on real native close, Cancel preserves the edit, Save-after-refusal succeeds, Discard quits and a relaunch confirms the draft was never persisted); navigation-away via `ProjectSettings`'s "← Dashboard" link, once the two bugs above were fixed; and both Cmd+Q and in-app close on `SprintList` (Cmd+Q does not bypass the WebView to native termination, a second Cmd+Q correctly re-arms the guard with the newer edit visible, and a relaunch confirmed the saved data persisted). **Verified scope: window-close on `StakeholderManager` and `WorkItemEditor`, navigation-away on `ProjectSettings`, and both close paths on `SprintList`, all on darwin/arm64, unsigned local build.** Sign-out is not a live risk here — confirmed by grep that `AppHeader` (Sign out's only render site) never renders while any dirty-registering view is on screen, so it is structurally unreachable rather than merely untested. The mid-save silent-overwrite fix in `ProjectSettings` is unit-verified only; a mid-save edit cannot realistically be timed through GUI automation. Not GUI-exercised: Cmd+Q was only driven through `SprintList` as a representative case (traced to converge on the identical Go-side path as window-close, but not independently driven through every editor), and Linux/Windows builds were not tested — no host available. `Dashboard.svelte`'s `close()` calls the backend `CloseProject()` before its own guarded navigation; this is safe today only because `Dashboard` itself never registers a dirty draft, flagged for awareness rather than fixed. |
| Validate package lifecycles | In progress — scoped to darwin/arm64 `.dmg`/`.pkg`, unsigned local build | 2026-08-16: built both macOS package formats (`make package-macos`, `make package-macos-installer`) and inspected them read-only: `pkgutil --expand-full` on the `.pkg` confirmed no postinstall scripts and, via `lsbom`, that every payload path lives under the app bundle with nothing written outside it — structural proof that install/removal cannot touch user data under `~/Library/Application Support/GoPMgr`. `hdiutil attach -readonly` on the `.dmg` confirmed the standard drag-to-Applications layout. Installed the `.pkg` payload to `/Applications` and ran a real isolated first-run: the data root was created correctly, existing user data was untouched, the process exited cleanly, and removing the installed bundle left user data intact. A later session relaunched the installed binary against the same isolated data directory and confirmed the account and previously-saved project survived the relaunch. Not tested: upgrade and reinstall (no receipt-tracked `installer -pkg` run — would require root, not requested), Linux/Windows package formats (no host available), and receipted removal via `pkgutil --forget`/Finder trash (only a file-level equivalent was exercised). |
| Confirm `Login` cannot leave a stale session's project open under a new user's key | Investigated, closed — see `app_session_test.go` | Found 2026-08-16 while enumerating every write site of `a.dek` (`app_session.go`) during an unrelated investigation: `App.Login` set `a.user` and `a.dek` to the newly-authenticated user's values but never checked whether `a.db`/`a.dbPath` were already set from a previous session — unlike `Logout` and `shutdown`, which always clear `a.db` and `a.dek` together in the same locked section. Tracing the frontend's login flow found this is not reachable through the shipped UI: the only path into the login screen from a post-login, project-open state is `AppHeader`'s sign-out button, which already calls `Logout()` (closing `a.db`) before switching views; the other two entry points into the login flow (`recovery_reset`, `create_account`) are reachable only from the login screen itself, where `a.db` is already nil. Closed anyway with a defense-in-depth guard in `Login`, checked once in the same critical section as the assignment it protects. The check deliberately runs *after* credentials are verified and a DEK is derived, not before — an earlier check would leave a window for a concurrent `OpenProject` to set `a.db` while `Authenticate`/`UnlockDEK` are still doing password-hashing work. The DEK derived on a refusal is zeroed immediately (ADR-001). A refused login still stamps the account's `last_login`, since `Authenticate` writes that before this guard runs; this is a known, accepted side effect — avoiding it would mean reopening the race, and the guard is cheap enough that it's worth keeping independent of frontend behavior. `TestLoginRefusesWhileProjectOpen` (`app_session_test.go`) proves the refusal, and that the original session's user and live database connection are untouched by the attempt. |
| Native-verify the Cost Control Phase 1 flow | Done — isolated darwin/arm64 Wails development build | 2026-08-20: created a disposable local administrator and project in an isolated data root, then recorded a planned ledger entry, contingency and management reserve balances, and an immutable baseline through the real native UI. The runtime showed the intended equations exactly: base plan USD 800.00 + contingency USD 100.00 = cost baseline USD 900.00; baseline + management reserve USD 50.00 = authorised funding USD 950.00. The confirmation dialog was visible before baseline approval, and the resulting history retained the local approver, machine-readable UTC timestamp, rationale, baseline, and authorised total. An earlier one-second observation labelled a login action as pending, but the sign-in control had remained disabled because accessibility `set_value` did not emit the Svelte input events; it was an automation-input limitation, not a product request or session hang. A component-level submit guard and regression tests now protect duplicate login events independently of the disabled control. Native Windows, VoiceOver, and keyboard-only coverage remain planned separately. |
| Native-verify chosen-location and financial-report exports | Done — isolated darwin/arm64 evidence for the scoped financial export | Document, combined-report, signed, GnuPG, and schedule exports now prompt desktop users for a new destination; existing files and report `.manifest.json`/`.asc` sidecars are refused rather than replaced. Selected parent directories are not retained in project audit data. The new Cost Control action exports a printable, project-scoped PDF that separates Legacy Budget context from Cost Control ledger, reserves, and baseline history. Automated tests cover private new-file publication, collision preservation, cancellation/extension handling, snapshot arithmetic, PAdES audit path redaction, and the UI action. On 2026-08-21, the guarded launcher used a built app with a disposable administrator/project; a real Save panel was cancelled and left the retained export inventory unchanged, then a new PDF was saved, checked with `pdfinfo` and extracted text, rasterized page by page, opened in Preview, and taken as far as Preview's Print dialog before cancelling without sending a print job. The raster review found a Cost Control ledger-cell overflow; `internal/documents/financial_report.go` now fits every cell to fpdf's actual inner width, keeps canonical signed-`int64` money unabridged in the amount column, and constrains all three report tables to the A4 landscape content width. The rebuilt native app re-exported the retained project and both corrected pages were inspected without overlap or right-margin overflow. Other export families (charts, calendar, audit, Sigma) retain their current location behavior and are not covered by this slice. |
| Confirm self-heal is reachable on real encrypted corruption | Investigated, closed — see `repair_and_swap_test.go` | `App.RepairAndSwap()` requires `a.db != nil`, which requires `OpenProject` to have already succeeded — every project is SQLCipher-encrypted. An initial single-offset probe found `OpenProject` itself failing on the exact light-corruption fixture `internal/db/repair_selfheal_test.go` uses, raising the concern that `RepairAndSwap` might be unreachable exactly when it's needed. A systematic sweep — one byte flipped per 4096-byte page across a 105-page encrypted fixture, `OpenProject`/a real query/`RepairAndSwap` recorded per page, byte restored before the next trial, reproducible across repeated runs — resolved it: `OpenProject` succeeded after single-page corruption on 80 of 105 pages (76%); the earlier failure was one unlucky page (page 1, hit by the schema/settings bootstrap), not a systemic problem. This sweep covers single-page corruption only — not multi-page corruption, torn writes, or truncation — so the closure is scoped accordingly. Within that scope, `RepairAndSwap` is reachable in the large majority of cases: `TestRepairAndSwapHealsReachableLightCorruption` pins one such case end-to-end (open succeeds, a real query succeeds, `RepairAndSwap` detects and heals the corruption during its own `VACUUM INTO` pass). Of the 3 pages (out of 105) where corruption was severe enough that a query failed even after a successful open — the scenario `RepairAndSwap` actually targets — its own healing attempt also failed on all 3 in this sample rather than producing a healthy copy; `TestRepairAndSwapCanFailToHealEvenWhenReached` pins this as current, real behavior, not a bug (no test can require `VACUUM INTO` to heal arbitrary corruption). This mirrors the same light-vs-severe distinction already documented for plaintext SQLite, and confirms it holds on the encrypted path too. The small sample (n=3) suggests user-visible corruption may skew toward the unhealable side more than the overall 76/24 split implies — worth revisiting with a larger sample if this becomes a real support signal — but no code change was needed to close this item, since `RepairAndSwap` was already doing the right thing when reachable. |

## P1: release trust and usability

| Item | Status | Completion evidence |
| --- | --- | --- |
| Sign and notarize distributables where supported | Blocked | Release credentials are configured and the published artifacts pass the platform trust checks. |
| Configure the signed update channel | Blocked — missing signing key, not a code/test gap | The update client (`internal/update`) and CI wiring are already complete and heavily tested. `.github/workflows/release.yml` injects `CurrentVersion`/`UpdateChannel` via ldflags on every build, and additionally injects `ManifestURL`/`UpdateChannelPublicKey` whenever the `GOPMGR_UPDATE_PUBLIC_KEY` secret is set; the release workflow signs a per-platform channel manifest with Ed25519 whenever that secret (and its private-key counterpart) is present. `internal/update/check_test.go`'s 54 tests cover signature verification, channel/platform/architecture/semver/URL/digest/timestamp validation, downgrade refusal, and graceful non-blocking failure when the update server is unreachable or unconfigured — confirmed by grep that neither the CLI nor the GUI startup path blocks on this check. `gh secret list` confirms that neither `GOPMGR_UPDATE_PUBLIC_KEY` nor `GOPMGR_UPDATE_PRIVATE_KEY` exists as a repository secret. With both unset, every release build takes the `ManifestURL == ""` branch and ships with the update channel disabled, fail-closed, exactly as designed. This is the same class of blocker as the other two `Blocked` rows in this table (signing/notarization, PAdES): a missing credential, not missing code or test coverage. Unblocking requires generating an Ed25519 keypair and adding it as GitHub Actions secrets — an account-level action outside a code change. |
| Obtain trusted PAdES evidence | Blocked | A release-certificate sample validates as trusted and independent interoperability evidence is archived. |
| Complete keyboard and assistive-technology smoke coverage | In progress — `Tabs.svelte`'s browser-tab half now closed 2026-08-25; native window-chrome half explicitly deferred, not attempted | `ConfirmDialog` now moves focus to its least-destructive Cancel action on open, traps forward/reverse Tab navigation, restores a connected trigger after close or parent destruction, and retains focus on the dialog while busy disables every control. Its 12 component tests include a fault-seeded initial-focus check. The Help reference now limits those guarantees to Signature Options and shared confirmation prompts; it no longer claims every dialog behaves this way. **2026-08-25**: closed the specific gap the R3/R4 rows above disclosed ("keyboard/focus behavior is verified by assertion... rather than a real focus ring in a real window") for `Tabs.svelte`'s two consumers (Dashboard's 4 tabs, Project Settings' 5). Prior automation in this environment could only dispatch synthetic `KeyboardEvent`s via `dispatchEvent` — real (`isTrusted`) OS-level key delivery to a Browser-pane tab had been unreliable in earlier sessions. This time, focusing the target element via `javascript_tool` first, then sending the key through the harness's real OS-level input path (not `dispatchEvent`), delivered a genuinely trusted event: `document.hasFocus()` flipped from `false` to `true`, and `:focus-visible` matched with the exact rendered outline the CSS defines — confirmed in both themes: dark (default), `solid 2px rgb(34,211,238)` matching `app.css:23`'s `--focus-ring`; light (`data-theme="light"`), `solid 2px rgb(14,116,144)` matching `app.css:84`'s override. Exercised ArrowRight/ArrowLeft (both directions' wraparound), Home, End, and Tab against both consumers — Tab correctly exits the tablist into its `tabpanel` rather than cycling tabs, matching the roving-tabindex design — all correct, zero defects. This closed the "do the DOM assertions correspond to a real focus ring" half of the gap. It did **not** close the other half — whether the native WKWebView desktop window's own chrome intercepts these keys differently — because the tests ran through a plain Chromium tab hitting the `wails dev` asset server (`localhost:34115`), not the actual launched `gopmgr.app` window. A same-session attempt to test the real native window via computer-use (the running `dev.gopmgr.GoPMgr` process) was made and the user explicitly denied the access-approval prompt — a stated boundary, not a technical failure, so this is not retried automatically. First run, project editing, reports, native window controls, and VoiceOver/NVDA/Narrator all remain unrun. |
| Replace remaining `window.confirm()` dialogs with the app's own modal | Done — all known `confirm()` call sites in the app now route through a real DOM modal | Root cause confirmed 2026-08-17: the vendored Wails v2.13.0 source this repo pins (`internal/frontend/desktop/darwin/WailsContext.m`) declares conformance to `WKUIDelegate` but implements none of the `runJavaScript{Alert,Confirm,TextInput}Panel...` methods, so there is no delegate to present a native panel. In a packaged macOS build, `window.confirm()`/`alert()`/`prompt()` silently produce no dialog and the calling code proceeds as if it got a falsy/no-op result. This was first noticed as a side effect during native-close guard testing: `StakeholderManager`'s in-form Cancel/×/Escape controls silently no-op'ed on a dirty draft — not a data-loss issue, since the shared native-close guard's dirty registration survived the failed dismissal, but a stuck-UI usability bug. Fixed in two rounds the same day. First, the three "discard unsaved changes" confirms (`StakeholderManager`, `WorkItemEditor`, `SprintList`) were routed through the existing shared Save/Discard/Cancel guard the native-close path already uses. Second, six remaining plain yes/no `confirm()` sites — delete stakeholder, delete work item, complete/delete sprint, delete deployment (`DORADashboard`), and replace-categories preset (`FishboneEditor`) — were all silently inert in a packaged build. Fixed by building the app's first shared UI primitive beyond `Spinner.svelte`: `frontend/src/lib/components/ConfirmDialog.svelte`, a real `role="alertdialog"` modal, wired into all six sites. `SprintList.complete()` uses a caution tone rather than the danger styling used elsewhere, since completing a sprint has no reopen path anywhere in the app (confirmed by grep) — irreversible in effect, though not a delete. `StakeholderManager.destroy()`'s pre-existing guard against deleting a row while another draft is open or a save is in flight was preserved unchanged. Each site has a regression test with a spy asserting `window.confirm` is never called; `ConfirmDialog.test.ts` proves Escape/Cancel/Confirm behavior independently of `window.confirm`'s return value. `DORADashboard.test.ts` and `FishboneEditor.test.ts` are new files with no prior coverage of any kind, scoped to the paths fixed here. Verified at the source/jsdom level plus one live spot-check (create and delete a stakeholder end-to-end through the new dialog in a running `wails dev` instance) — not re-driven through a packaged build this cycle. |
| Fix launchpad wizard promising the Software-Dev Pack but leaving it disabled | Done — see `TestCreateProjectFromLaunchpadEnablesAgilePackWhenSeeded`, `TestCreateProjectFromLaunchpadLeavesAgilePackDisabledWithoutAgileSeeds`, and `TestApplyLaunchpadSeedsLeavesAgilePackDisabledAfterPartialFailure` (`encryption_project_test.go`) | Found 2026-08-17 during a UX audit, then root-caused and fixed the same day. The New Project wizard's "Starter Artifacts" step defaults `kanban`/`backlog`/`sprint1` to checked for Scrum-ish methodologies, which reads as a promise that the Software-Dev Pack will be active on creation. `Seeder.Apply` seeded those rows correctly, but never set the separate `Settings.AgileEnabled` flag the dashboard gates the pack's visibility on (defaults false) — so a Scrum user landed on a project with real seeded data sitting behind a section labeled "disabled," only reachable via a manual toggle after scrolling past the rest of the tool catalog. Fixed in `CreateProjectFromLaunchpad`: after seeding completes with no error, and if the requested seeds included any of `kanban`/`backlog`/`sprint1`, the code now loads settings, sets `AgileEnabled = true`, and saves — using the newly-created project's still-open database handle directly, since `OpenProject` hasn't run yet at that point. A settings read/write failure here folds into the existing seeding-error path rather than being swallowed, matching how a seeding failure was already surfaced; an earlier version of this fix silently discarded both error returns before review caught it. Two Go tests cover the successful and no-agile cases. `TestApplyLaunchpadSeedsLeavesAgilePackDisabledAfterPartialFailure` uses real SQLite triggers to force failures before, within, and after agile seeds; it proves partial receipts are preserved, later seeds never run, and `AgileEnabled` remains false. The test is fault-seeded by removing the `seedErr == nil` guard, which makes all three cases fail. Live verification remains the prior successful-Scrum creation only; partial-failure UI messaging is backend-tested, not GUI-driven. |
| Extract a shared Button/Card/Input/Textarea/Select component library (design-critique Priority #2) | Foundational library done, 9 files touched as proof-of-concept (specific call sites only, not whole-file conversions); 68 of the app's 77 remaining screen files still contain at least one raw `<button>`/`<input>`/`<textarea>`/`<select>` element deliberately out of scope this cycle | A 2026-08-17 UX audit found the app had no shared component library beyond `Spinner.svelte`: roughly 217 hand-copied `<button>` class strings across 6+ divergent visual families (including four Sigma module files running an entirely separate, hardcoded-hex Bootstrap-style button system that ignored the app's light/dark theme), ~307 `<input>`/`<textarea>`/`<select>` tags sharing one 90%-duplicated class string with inconsistent styling, and ~40 card/panel variants including a byte-identical duplicate wrapper in `ChartCatalog.svelte`/`DocumentCatalog.svelte`. Built five real Svelte 5 components (`Button`, `Card`, `Input`, `Textarea`, `Select`) in `frontend/src/lib/components/`, using `$bindable()` plus a `...rest` prop spread so native HTML attributes pass through unchanged — the main technical risk, given the variety of attributes across ~307 call sites. `Button`'s `sm`/`md`/`lg` sizes map to the three largest padding buckets actually found in the codebase (71% of sized buttons combined), not an invented scale; the `link` variant is exempt from sizing since it appears inline in prose and forcing it through the shared base classes would disturb surrounding text; a `ghost` variant was added after a mid-implementation check found `ConfirmDialog`'s own Cancel button didn't match any first-draft variant — two tests in `Button.test.ts` reconstruct `ConfirmDialog`'s original class strings token-for-token to prove the migration changed nothing visually. `Card` stayed static-only (no clickable/polymorphic mode) after review flagged that the app's card-shaped clickable dashboard tiles are full custom rows whose click-handling behavior a generic wrapper risked breaking on the app's most-visited screen; the proof-of-concept instead targeted the lower-risk, confirmed-duplicate `ChartCatalog`/`DocumentCatalog` case. `Input`/`Select` bake in `w-full mt-1` layout, since Tailwind resolves conflicting utility classes by stylesheet position rather than class order, so `class` passthrough can't reliably override them — this would have silently broken `StakeholderManager`'s inline header filter field, which was dropped from the migration scope rather than forced. The color palette was taken from the literal, most-used Tailwind classes rather than the app's `accent`/`critical` design tokens, after checking that the token's actual resolved color didn't match — adopting it would have silently recolored every migrated button. Migration was a scoped proof-of-concept, not exhaustive: all four Sigma files (11 of 12 button sites; the 12th, a circular corner-badge button, was left as local CSS with only its hardcoded color corrected), `ConfirmDialog.svelte` (dogfooding), `StakeholderManager.svelte` (3 of 7 buttons plus all form fields — the nav-back link, two icon-glyph buttons, and a full-row click target were left as distinct patterns), `ChartCatalog.svelte`/`DocumentCatalog.svelte`, and all 23 `link`-variant cross-reference buttons in `HelpFeatures.svelte`. Live-verifying the Sigma files hit a genuine, unrelated backend bug — DMAIC project creation was broken by a foreign-key mismatch, fixed separately (see the row below) — so those four files were visually verified at the time using a temporary, uncommitted Vite harness that mounted each component with a stubbed backend, deleted after use. `StakeholderManager` was verified inside the real running app instead (create, save, delete a stakeholder end-to-end). `make verify` (Go tests, `svelte-check --fail-on-warnings`, the full frontend vitest suite, and the production build) passed clean throughout. Not done this cycle: the remaining 68 files' worth of duplicated button/input/card patterns, and a separate Dashboard information-architecture restructuring proposed by the same UX audit — both deferred to their own planning cycles rather than rushed without adequate regression coverage. |
| Reconcile macOS package version-string sourcing | Done for macOS (darwin); Linux deliberately not applied | Spotted 2026-08-16 while inspecting build artifacts: `scripts/package-macos.sh` and `scripts/package-macos-installer.sh` derived their filename version differently. Root cause: both scripts' fallback used `git describe --tags --abbrev=0`, which silently reuses the nearest tag's bare name for a checkout any number of commits past it, so two different trees could produce identically-named local build artifacts. Fixed by extracting the derivation into `scripts/package-version-lib.sh` (`release_version()`), used by both scripts: an explicit `VERSION` env var wins (what CI always sets, so the real release pipeline was never exposed to this bug), else `git describe --tags` *without* `--abbrev=0` so a build past its tag gets an honest `-<N>-g<sha>` suffix, else `0.0.0`. Verified by rebuilding both artifacts at the same commit and confirming the filenames now agree exactly; the `.pkg`'s actual `PackageInfo` version (the real upgrade-comparison key, read from the app bundle's Info.plist) was unchanged. `scripts/package-version-lib_test.sh` proves the fix and is wired into `make verify` and both packaging targets; `scripts/package-macos_test.sh`, a pre-existing test invoked by nothing (not the Makefile, not CI), was also wired in alongside the fix. `scripts/package-linux.sh` carries the identical bug and is deliberately left unfixed. Later the same day, the pinned `nfpm@v2.47.0` was installed locally and both Linux packagers were run against a hyphenated dev version: both succeeded, with the hyphen sanitized into a legal RPM/Debian version string (`~` in place of the first `-`). This confirms the underlying concern was real — but what's still unverified, and now the actual blocker, is whether `rpm`/`dnf` itself accepts the sanitized version and orders it correctly relative to a plain release version; no `rpm` binary or Linux host was available to check. Applying the same fix to `package-linux.sh` on this evidence alone was considered and declined, since CI sets `VERSION` explicitly for this script too (so the bug is local-dev-only) and this host can't even build the Linux binary to test it — the fix should land in a cycle with a real Linux host to verify install/upgrade ordering end to end. |
| Close residual risks left by the Sigma DMAIC FK fix | Done | The Sigma DMAIC foreign-key fix (see the Sigma project-creation row below) closed with three residual risks disclosed rather than left open; this pass addressed all three. **Risk 1** (hardcoded corruption-fixture offsets in `repair_and_swap_test.go` are coupled to page layout): accepted as documented, no code change — the fixture's own doc comment already carries the re-derivation methodology and has since been used successfully twice, most recently after this same FK fix shifted the fixture's page layout. **Risk 2** (`SigmaListProjects` returned every `sigma_projects` row unfiltered by `project_id`): fixed by threading the currently-open project's ID through `SigmaListProjects`/`ProjectService.ListProjects`/`App.SigmaListProjects`, adding a `WHERE project_id = ?` clause matching the scoping pattern already used elsewhere in the package. An initial attempt used a schema-level trigger, which was validated in isolation but reverted after a full test run showed it broke ten pre-existing tests that deliberately exercise multiple `project` rows per database file. Review during implementation found the fix as originally scoped was too narrow: `SigmaProjectView.svelte` fetches a specific Sigma project by id directly (`SigmaGetProject`), bypassing the list entirely, with no equivalent check. That method was scoped too, since it's also the load-bearing entry point for six Charter/Fishbone/Solutions/ControlPlan/SIPOC/VoC sub-getters. The regression test written for that fix at the time turned out to be flawed — it used two separate `.gopmgr` files, which are independent SQLite databases, so the assertion passed on ordinary cross-database not-found behavior regardless of whether the ownership check existed. This was caught and corrected in a later cycle (see the row below), which also closed the six sub-getters plus four more methods that had the identical gap — all 17 App methods that take a bare Sigma project id are now scoped and verified against a same-file, multi-project fixture, with fault-seeding confirming the guard is load-bearing. **Risk 3** (sub-tab UI render under a Sigma-project switch was verified only at the save layer): closed by static code trace rather than a live run — `SigmaProjectView.svelte`'s only exit routes are two calls to `goto('sigma_dashboard')`, and the app's routing effect depends only on `session.view`, so any path into a *different* Sigma project necessarily tears down and remounts the view (re-running its mount logic against the new id) rather than reusing a stale instance. No direct sigma-project-to-sigma-project navigation path exists in the current UI to create the stale-render scenario the original risk described. |
| Close the remaining Sigma cross-tenant scoping gap; Priority #3 Penpot board | Done | Continuation of the row above's Risk 2 closure. The previous cycle's regression test (`TestSigmaGetProject_RejectsSigmaProjectFromAnotherFile`) turned out to be vacuous: it used two separate `.gopmgr` files, which are independent SQLite databases, so the id from one file's Sigma project could never collide with another file's regardless of whether the ownership check worked. Replaced with a same-file, two-project-row fixture, seeded via raw SQL matching the pattern this package's other `project_id`-scoping tests already use, then fault-seeded to confirm the assertions are load-bearing. Extended the fix from the 2 originally-scoped methods to all 17 App methods that take a bare Sigma project id — review flagged that the prior cycle's "6 sub-getters" framing missed `SigmaAdvancePhase` (a write), `SigmaCheckReadiness`/`SigmaGetToolStatus` (aggregating reads), and `SigmaExportProjectReport` (the widest read, since it exports a whole project's report to disk). Refactored to one shared `requireSigmaProjectInOpenFile` helper in `app_sigma.go`, replacing `SigmaGetProject`'s own inline check, so there is exactly one implementation of the rule. Coverage was cross-checked by grepping the full `Sigma*` method list against the helper's call sites: 17 guarded, 5 correctly excluded (2 don't take a bare Sigma id; 3 stats functions take no project id at all). Also built the Priority #3 Dashboard IA proposal board in Penpot, matching the existing boards' color/typography conventions and verified visually via `export_shape` before being called done — this caught one real issue, a `→` character rendering as a near-blank glyph in the project's font, fixed by using `->` instead. `make verify` passes clean; no Go method signatures changed (only new possible error returns), so no frontend changes were needed. |
| Priority #3 Dashboard IA restructuring — R1/R2 (naming collision, chart-fallback view) | R1/R2 done; R3/R4 (the tabbed restructuring itself) deliberately deferred to its own cycle | First two of four risks identified in `docs/design/dashboard-ia-restructuring-proposal.md`, closed this cycle; the other two (the tabbed restructuring itself) were deliberately scoped to their own cycle, reasoned below. **R1 (naming collision)**: three screens informally shared the word "dashboard" — the nav labeled the portfolio view "Dashboard," `Portfolio.svelte`'s own heading read "Portfolio dashboard," and a separate per-project dashboard view existed alongside both. Fixed by relabeling the nav item and heading to "Portfolio," plus two Help-page prose references that echoed the old name. 4 new tests cover the nav label, `aria-current`, admin-item gating, and the heading text. **R2 (`'charts'` registry-drift fallback)**: a real, already-reachable state hit when a saved chart's `kind` has no entry in the frontend's chart-route map — previously falling through to a generic "Unknown view" screen with no explanation. Built `ChartFallback.svelte`, which looks up the saved chart by id and names it in the confirmation copy on success, or shows a generic success message with a disclosure note on lookup failure — phrased as success rather than error, since both call sites only reach this view after the save has already succeeded. 3 new tests. Re-reading `Dashboard.svelte` during implementation, rather than trusting the design doc's prior claims, found and corrected four factual errors in the doc itself: a claimed visible heading that's actually screen-reader-only text; a "five kinds of content" count that was actually seven; a singular "Documents list" reference that's actually two separate lists; and a proposed tab gate on project methodology when the real toggle is unconditional. The Penpot board was rebuilt to match the corrected doc. R3/R4 (the actual tabbed restructuring) were deliberately not implemented this cycle: `Dashboard.test.ts` has 23 tests across 8 describe blocks, most touching content that would move under tab-switch logic, and the component has a documented pre-existing load-order race that would make mixing a DOM restructuring with a test rewrite risky to attribute failures correctly. Not live-verified in a running Wails build this cycle — the plain-vite dev server has no Go bindings — verified instead by the full vitest suite (223/223) and `svelte-check` (clean), plus `make verify`. |
| Priority #3 Dashboard IA restructuring — R3/R4 (tabbed restructuring) | Done | Continuation of the row above, implemented in its own cycle as planned. `Dashboard.svelte`'s seven content groups were restructured into an always-visible header meta-card strip plus a four-tab row (Overview default, Charts, Documents, Dev Tools), built on a new shared `Tabs.svelte` component implementing the W3C ARIA tablist pattern (roving tabindex, arrow-key and Home/End navigation). The pre-existing test suite's actual edit surface was measured, not estimated, by running it unmodified against the restructured component first: 12 of 23 tests failed (content that moved off the default tab), 11 passed unchanged (content reachable from Overview or the always-visible header). Each of the 12 got one tab-switch call inserted at its point of interaction; the load/toggle race test was fault-seeded after its edit (guard temporarily removed, confirmed the test still catches the regression) to prove the insertion didn't make it vacuous. Three implementation decisions were recorded as explicit corrections to the design doc: the loading/error indicator stays outside all four tab panels rather than nested in Overview, since the Charts/Documents catalogs depend on the same load; panels are conditionally rendered rather than kept mounted and hidden, since keeping all panels mounted would duplicate a chart-kind name in the DOM and break a pre-existing test; and a new "nothing created yet" empty-state message was added to the Overview tab, needed because an empty project's Overview tab would otherwise render nothing. 13 new tests (8 for the shared `Tabs` component's keyboard mechanics, 5 for the dashboard's own tab wiring). Not live-verified in a running Wails build — same disclosed limitation as the row above, but higher-stakes here since this changed the DOM structure of the app's most-visited screen; keyboard/focus behavior is verified by assertion (`tabindex`, `aria-selected`, `document.activeElement`) rather than a real focus ring in a real window. `make verify` passes clean. A follow-up item noted here (catalog search/filter state resetting on tab switch) was fixed the same week — see the row below. |
| Live-verify R3/R4 in a real Wails build; fix catalog filter-state reset; close the `Button` icon-variant gap | Done | 2026-08-18, continuing directly from the two rows above. **Live verification**: launched `wails dev` (real Go bindings, confirmed by the dev-server log line, unlike the plain-vite server used in earlier cycles) with an isolated data directory, and drove it through the browser — signed in, created a project, exercised all four Dashboard tabs, chart/document creation and deletion, and the Agile Pack toggle — zero defects found in the previously jsdom-only R3/R4 work. A later dev-server restart, done to capture screenshot evidence for Penpot, hit an environment guardrail against automated credential entry; recovered by reusing a still-authenticated browser tab from before the restart. An apparent regression (~46 "Canvas is already in use" console errors after extended interactive testing) was diagnosed and ruled out: a clean reload plus a fresh chart produced none, indicating accumulated test-session churn rather than a real defect in the chart rendering code. Coordinate-based clicks were not reliably registering in this dev environment (a viewport/screenshot scaling issue specific to the tooling), so further interaction used direct DOM element clicks instead — this exercises the real Svelte handlers but not the physical pointer-event path, disclosed as a methodology limitation. **Catalog filter-state fix**: `ChartCatalog.svelte`/`DocumentCatalog.svelte`'s expanded/search/filter state became bindable props, bound by `Dashboard.svelte`, so the state now survives the tab panels' remount on tab switch instead of resetting every time, as the R3/R4 design doc had disclosed and deferred. One fault-seeded regression test proves it. **`Button` icon-variant gap**: `Button.svelte` previously had no `...rest` prop spread, blocking the app's 21 icon-glyph "Remove" buttons from migrating. Added a `variant="remove"` (matching the existing icon-glyph footprint) and prop spread on all non-default variants. Migrated all 21 sites across 14 chart/document-editor files; one outlier (`RACIEditor`'s "Remove role" button) required converting its label from Svelte template syntax to a JS template literal, since component props are plain JS expressions unlike native attributes. 4 new tests plus a clean `svelte-check`; 2 of the 21 sites were live-verified directly in the running app, the other 19 were not individually verified since they render the same shared component code path. `make verify` passes clean; the `windows-installer-scaffold` target's pre-existing `makensis` crash — a known Homebrew/NSIS host issue unrelated to this change — was bypassed with `GOPMGR_SKIP_NSIS_COMPILE=1`, which skips rather than passes that check, so NSIS template syntax was not re-verified. Not done this cycle: the remaining ~68-file shared-component migration, and Project Settings' flat-form-to-tabs restructuring. |
| Fix Sigma DMAIC project creation (FK constraint made the module unusable) | Done | Found 2026-08-17. `SigmaCreateProject` generated a synthetic `sigma-<timestamp>` ID that never satisfied `sigma_projects.id`'s foreign-key constraint against `project(id)`, so every DMAIC project creation failed outright — the entire Six Sigma module was unreachable end-to-end. An initial proposed fix (reuse the open GoPMgr project's own ID as the Sigma project's ID) was investigated and found wrong: the schema forces at most one `sigma_projects` row per file under that approach, but the real UI creates and lists several DMAIC projects per file, so the fix would work for the first creation and then fail every subsequent one on a primary-key collision. Root-caused by comparing `sigma_projects`' schema against every other "many rows per project file" table in the schema (`charts`, `baselines`, `scenarios`): all of them use an independently-generated `id` plus a separate `project_id` column referencing `project(id)` — `sigma_projects` was the sole outlier, using its own primary key as the foreign key. Fixed by giving it the same shape (`project_id` column plus foreign key), adding `domain.Project.GopmgrProjectID` to carry it, generating `sigma_projects.id` via the existing `newID("sigma")` convention instead of a timestamp, and resolving the open project's ID in `SigmaCreateProject` following the same pattern `SaveChart` already uses. Since SQLite can't alter a primary/foreign key in place, existing files need a real migration (`migrateSigmaProjectsSchema`): it probes for the `project_id` column first (a no-op for files that already have it, including every new file), then rebuilds the table on a single reserved connection with foreign-key enforcement explicitly disabled and re-enabled around the rebuild (required because `PRAGMA foreign_keys` is per-connection state, and using a pooled connection risks running the rebuild on a different connection than the one that disabled it), and verifies the result with `PRAGMA foreign_key_check`. A second, previously undiscovered issue surfaced mid-fix: `EnsureProjectSigmaLink`, a function with its own test but zero production callers, encoded the opposite 1:1 design and would have silently no-op'ed under the corrected schema (SQLite's `INSERT OR IGNORE` swallows a `NOT NULL` violation rather than erroring). Rather than deciding unilaterally, this was raised with the user, who chose to delete the function since nothing calls it. Regression coverage requires two Sigma projects under one GoPMgr project to succeed with distinct IDs, both returned by `SigmaListProjects` (a weaker one-create-succeeds test would also have passed under the rejected fix), plus a dedicated migration test against a hand-built legacy-schema file proving both data preservation and idempotency across repeated opens (`sigma_migration_test.go`). The schema change shifted an unrelated fixture's on-disk page layout enough to break a hardcoded corruption-offset test elsewhere (`TestRepairAndSwapCanFailToHealEvenWhenReached`) — caught by a full test run rather than assumed unaffected, and the offset was re-derived using the same page-sweep methodology the fixture's own doc comment prescribes. Live-verified end-to-end in `wails dev` against an isolated account: created two DMAIC projects (one via the Go binding directly, one through the actual "Start New DMAIC Project" form), confirmed both render as separate cards in the workspace grid, and confirmed navigating into each independently loads its own Define-phase tab set correctly. `make verify` passes clean. |
| Project Settings tab restructuring; fix Methodology freeform-input inconsistency | Done | 2026-08-18. Two findings from the same design-critique pass that produced the Dashboard IA work above, both on `ProjectSettings.svelte`, addressed together. **Tab restructuring**: the screen's roughly 2,000-line, 10-section flat scroll became 5 tabs on the shared `Tabs.svelte` component — its first validation as a second consumer beyond Dashboard. Tabs: General (Identity/Classification/Lifecycle, the default), Scenarios, Resources, Exports & Signing, and Data Protection (backup and database encryption, deliberately kept separate from Exports & Signing so the two highest-stakes actions on this screen stay easy to find). A first attempt at reordering two sections during the refactor left duplicate DOM ids and a broken `aria-controls` target — caught and fixed before any test run, not shipped and found later. The header Save/Revert buttons stayed global (rather than per-tab) since `revert()` discards every field in `draft`, all of which live on the General tab, but were relabeled "Revert details"/"Save details" so their scope is clear regardless of which tab is active. The pre-existing 2-test suite's actual edit surface was measured by running it unmodified first (both failed, both live in a section that moved to a non-default tab) and exactly the needed tab-switches were inserted. **Methodology field**: was a bare freeform text input, inconsistent with the industry-scoped picker already used at project creation. Replaced with a `<select>`; since the underlying field has no server-side whitelist, an out-of-list current value is prepended as its own "(current)" option rather than silently dropped or changed by merely opening the select. The wizard's own methodology-options table was extracted to a shared `frontend/src/lib/methodologies.ts` so the two screens can no longer drift independently on what a given industry's options are. Also mirrors the wizard's existing behavior of clearing methodology when industry changes. 9 tests total (2 pre-existing, 3 new tab-wiring, 4 new methodology), one fault-seeded. Live-verified end-to-end in `wails dev`: created a project through the wizard, confirmed all 5 tabs render correct isolated content, confirmed the Methodology field renders as a dropdown, confirmed changing Industry clears it and enables Save/Revert, confirmed Revert restores both fields together, and confirmed a Scenario-name draft survives switching away and back. `make verify` passes clean. The Penpot board update for this change was deferred to the row below since the plugin's browser connection had dropped. |
| Penpot board update for the row above; close 5 more Priority #2 remove-variant sites | Done | 2026-08-18, continuing from the row above once the Penpot connection was restored. Built a new board documenting the Project Settings tab restructuring (matching the existing boards' conventions) and added a correction callout to the existing style-guide board, fixing two claims that had gone stale: the "Button has no `...rest` spread yet" gap note (closed two cycles earlier) and a proof-of-concept file-count footer that was never updated. Both verified visually before being called done. **Button migration continuation**: a broader re-grep for the icon-glyph "remove" button pattern (not just matching by `aria-label`) found it in `StakeholderManager.svelte`, `SprintList.svelte`, `DORADashboard.svelte`, and two sites in `DocumentFieldEditor.svelte` — one of which had a trailing class that made it miss the earlier grep. All 5 sites across 4 files migrated to `Button variant="remove"`, preserving each site's extra classes via passthrough. Existing tests for the 3 already-tested files passed unmodified (they query by label or text, not markup shape); one assertion pinning the migrated class string was added per file, each fault-seeded by reverting the migration and confirming it fails before restoring. `DocumentFieldEditor.svelte` had no prior coverage; a new, narrowly-scoped test file covers only the two branches touched here. Deliberately deferred, not silently dropped: `WorkflowEditor`/`ActivityEditor` ("Delete edge") and `CPMEditor` ("Remove assignment") share the identical pattern but have no existing test coverage and non-trivial mount state, so a render-plus-click test would prove imports work, not that the migration is safe. Deliberately excluded as distinct patterns: `GanttEditor`'s delete buttons use a different color, and `AdminPanel`'s red-hover link is a different text style entirely. Re-running the file-count inventory after all edits found it unchanged at 73 files (the "68" figure quoted in earlier rows was itself already stale by this point) — the 4 touched files each retain other raw elements outside this cycle's scope. `make verify` passes clean. |
| Migrate `WorkflowEditor`/`ActivityEditor`/`CPMEditor` to the shared `Button` component (Priority #2 continuation) | Done for the call sites with an unambiguous variant/size match; 3 files converted, 12 of 27 raw buttons migrated, 15 documented exclusions | 2026-08-19. Continuing the Button migration — this trio had been named as deferred scope across several prior cycles. Migrated each editor's load-error "Back to dashboard" and header "Save" buttons, `ActivityEditor`'s "+ Swimlane," `WorkflowEditor`/`ActivityEditor`'s "Delete edge," `CPMEditor`'s four resource-toolbar buttons, and its "Remove assignment" button — 12 of 27 raw buttons in these three files. A real bug was caught during the "Remove assignment" migration: the original button carried an extra text-size class that the `remove` variant doesn't include by default (a deliberate exemption, same as the `link` variant) — the first draft dropped it silently, caught by the same class-string-equivalence check established in an earlier migration cycle, before any test existed to catch it. Deliberately left as raw buttons in all three files, consistent with prior precedent: the "← Dashboard" nav link, "Connect…"/"Delete node" (a different opacity value than `Button`'s default), the "+ Node" dropdown menu items (not a button at all), and anything in the shared canvas-editor shell used by other editors outside this cycle's scope. `CPMEditor`'s Monte Carlo/PDF-export/EVM buttons use a padding value `Button`'s `sm` size doesn't match — migrating would be a visible padding change, not a pure refactor, so left unmigrated as a permanent exclusion pending a dedicated design decision (later made — see two rows below). 6 new tests (all previously zero-coverage files), pinning the exact rendered class string for each migrated variant/size combination reachable without canvas node-selection setup; the two `remove`-variant buttons that need a selected node/edge were instead verified by reading `Button.svelte`'s `remove` branch directly and confirming the pattern is already proven at 12 existing call sites elsewhere in the app. Full suite: 264/264 passing (up from 258), `svelte-check` clean, `eslint` clean on all touched files. Remaining scope: the rest of the original ~68-file inventory beyond this trio. |
| Extend `Button` with `canvas`/`canvas-danger`/`compact` to close the row above's remaining `WorkflowEditor`/`ActivityEditor`/`CPMEditor` exclusions | Done for the 6 call sites this closes; a real, grep-confirmed app-wide idiom now unblocked (not migrated) in 3 more files | Continuing directly from the row above, closing the API gap it named as next work. A fresh app-wide grep found the `disabled:opacity-30` pattern excluded from the prior migration is not a one-off: it's a real, consistently reused idiom across 5 files for canvas-toolbar action buttons (`WorkflowEditor`, `ActivityEditor`, `WBSEditor`, `CauseEffectEditor`, and the shared canvas-editor shell — 13 occurrences), distinct from the 113 occurrences of the standard opacity value used everywhere else. Similarly, the odd padding value used by some `CPMEditor` buttons turned out to be a real fourth padding bucket used at 10+ sites app-wide, not just in that one file. An initial design proposed a separate `disabledOpacity` prop as an independent axis; review rejected it, since every real call site pairing the 30% opacity value is already in the canvas-toolbar family, and the prop would make unsupported combinations representable. Took the smaller-surface alternative instead: relocated the disabled-opacity class out of `Button`'s shared template into each variant's own class list (verified as a no-op via the full pre-existing test suite passing unchanged before any new variant was added), then added `canvas`/`canvas-danger` variants and a `compact` size. Migrated `WorkflowEditor`/`ActivityEditor`'s "Connect…" and "Delete node," and `CPMEditor`'s "Compute" and "Run" buttons. 4 new `Button.test.ts` cases plus one each in the three consumer test files. The two canvas-variant tests specifically assert the *default, no-selection* state (not merely the disabled state), since the opacity class token is present in the DOM regardless of the `disabled` prop's value — only its visual pseudo-class effect is gated — so what actually needed proving was that the correct class renders on mount, before any canvas interaction. Fault-seeded by temporarily corrupting the `canvas-danger` variant's hover color and confirming three tests across three files failed with the expected diff, then restored. Also found and fixed six pre-existing exact-string test assertions whose expected class order shifted (not their content) as a side effect of the relocation — switched to an order-independent comparison, since class order was never semantically meaningful. A production build was checked to confirm Tailwind's CSS generation correctly picked up the new literal class combinations (a naive string-interpolated class name would have silently failed to generate). Full suite: 269/269 passing, `svelte-check`/`eslint` clean. Not done this cycle, named as next work: `WBSEditor`/`CauseEffectEditor`/the shared canvas shell's other consumers share the same idiom but weren't part of this row's scope; `CPMEditor`'s "Export PDF/A" uses yet another distinct padding value and was left excluded pending its own decision (see below). |
| Migrate `WBSEditor`/`CauseEffectEditor`/`_layered_editor_shell.svelte` to `canvas`/`canvas-danger`, closing the row above's remaining exclusions | Done — all 3 files, 5 call sites | Continuing directly from the row above. Migrated `WBSEditor`/`CauseEffectEditor`'s single "Delete" buttons and the shared canvas-editor shell's "Connect…"/"Clear edges"/"Delete node" buttons — no `Button.svelte` changes needed, purely applying the variants built above. The shared shell is also used by `NetworkEditor` and `PERTEditor`, neither of which had any prior test coverage — migrating it changes two untested consumers at once. Closed by adding a new test file mounted through `NetworkEditor` (the thinner of the two), pinning all 3 migrated buttons' class strings; `CPMEditor`'s existing 8 tests were re-run unmodified to confirm the shared-file edit didn't regress the one consumer that already had coverage. A planning-stage assumption that the shell's buttons were isolated to the two untested consumers was corrected before writing any code, once re-reading the template showed the shell always renders its own default toolbar for `CPMEditor` too, alongside `CPMEditor`'s own separate toolbar content. Deliberately left out of scope: `WBSEditor`/`CauseEffectEditor`'s "+ Child"/"+ Sibling"/Save buttons, a different always-enabled pattern with no disabled/opacity styling at all. 3 new test files (all previously zero coverage), one fault-seed (reverted the shell's "Delete node" variant, confirmed the new test catches it, restored). Full suite: 272/272 passing, `svelte-check`/`eslint` clean. `CauseEffectEditor.test.ts`'s evidence for the shared-pattern class is inferred from the shell's own fault-seed (same class family) rather than independently seeded on that file — noted explicitly rather than implied as independent evidence. |
| Migrate `WBSEditor`/`CauseEffectEditor`'s "+ Child"/"+ Sibling"/"Save"; add `PERTEditor` its own direct test | Done — 6 more call sites, 1 new test file | Continuing from the row above, plus an explicit request to add `PERTEditor` its own direct test. Migrated `WBSEditor`/`CauseEffectEditor`'s "+ Child"/"+ Child cause"/"+ Sibling" (`variant="secondary" size="sm"`, no `Button.svelte` change needed — both buttons had no `disabled` attribute at all pre-migration, so the disabled-opacity class is inert on them) and "Save" (`variant="primary" size="sm"`, a real `disabled={saving}` state). `PERTEditor.test.ts` is the first test to mount `PERTEditor` directly — the earlier shared-shell test only covered the shell through `NetworkEditor`, leaving `PERTEditor` with inferred rather than direct coverage. Fault-seeded independently (renamed a button's label in the shell, confirmed this new test fails via a missing-element query, restored), proving it exercises the shell through `PERTEditor`'s own mount path rather than duplicating prior evidence. 2 more tests added to the `WBSEditor`/`CauseEffectEditor` suites, each independently fault-seeded. Full suite: 275/275 passing, `svelte-check`/`eslint` clean. Noted in passing but not migrated: `WBSEditor`'s load-error "Back to dashboard" button is an exact match for the pattern already migrated elsewhere — flagged as an easy next candidate. |
| Migrate `WBSEditor`/`CauseEffectEditor`'s load-error "Back to dashboard"; add `NetworkEditor`'s own direct test | Done — 2 call sites, 1 new test file with real new coverage | Continuing from the row above. Migrated `WBSEditor`/`CauseEffectEditor`'s load-error "Back to dashboard" button (byte-identical in both files to the pattern already used elsewhere); one new test per file forces the load call to reject to reach that branch, each fault-seeded independently. The prior cycle's plan called the intended `NetworkEditor.test.ts` a mirror of `PERTEditor.test.ts`, but that framing was wrong: `NetworkEditor`, unlike `PERTEditor`, was already the mount target the shared-shell test uses, so a new file re-asserting the same shared-shell button classes through the same mount path would have been a duplicate assertion, not new coverage — caught before writing any code. Redirected to real, previously-uncovered ground instead: `NetworkEditor`'s own node-content and node-detail-panel rendering, which no test had ever exercised. The new test mounts with a real node and layout entry, asserts the node's label/duration/owner render unconditionally, and that the detail panel's duration input is absent before selecting the node and present with the correct value after. Both assertions independently fault-seeded (renamed a label inside each, confirmed each test fails only on its own seed, restored). Full suite: 279/279 passing, `svelte-check`/`eslint` clean. |
| Add `Button`'s `nav` variant; migrate the header "← Dashboard" nav link in 13 already-tested files | Done for the variant and 13 files (15 of 28 call sites); 13 zero-coverage files deliberately deferred to their own cycle | Continuing from the row above's named next work — whether the `← Dashboard` nav-link pattern's call-site count justified a new `Button` variant. A grep found the exact pattern (`text-xs text-slate-400 hover:text-cyan-400`, no padding) at 28 sites across 26 files — a real, consistently reused idiom, distinct from the existing `link` variant's color and from a separate 3-site underlined variant on `AppHeader`/`Dashboard`, left as its own idiom. None of the 28 sites carry any attribute beyond `onclick`/`class`, so the swap is a pure class-string substitution. Review split the planned scope: 13 of the 26 files had zero prior test coverage and would each need a bespoke mount harness (agile components need different session-state shapes than chart editors; the project-creation wizard has step-gated back buttons), which was identified as the highest-risk part of the plan. Split into this cycle (the new `nav` variant plus the 15 occurrences in the 13 already-tested files) and a later cycle (the other 13 files). Added `Button`'s `nav` branch (exempt from the shared base like `link`/`remove`) and migrated 15 occurrences across 13 files, including the project-creation wizard's three step-gated buttons (Cancel plus two mutually exclusive "← Back" buttons, each needing its own test from the step where it actually renders). 18 new tests total. Fault-seeded twice: once on `Button.svelte`'s `nav` branch itself (confirmed both the component test and a consumer test fail identically, restored), and once by mis-setting one wizard step's back button to a different variant (confirmed only that step's own test failed, not the other two, proving the step-gated tests actually discriminate). The other 11 files' own tests rely on the shared `Button.svelte` seed rather than an independent one each — stated explicitly here rather than implied as independent per-file evidence. A completeness grep after the swap confirmed exactly 13 occurrences remain in exactly the 13 deferred files. A stale test-count note in the coverage ledger was corrected in passing. Full suite: 297/297 passing, `svelte-check`/`eslint` clean. No live/GUI verification — a pure class-string swap with no bound attributes makes exact-class-equality plus `svelte-check` the appropriate evidence tier here. Not done this cycle: the 13 zero-coverage files, and the 3-site underlined idiom, left as a separate, smaller pattern. |
| Build test harnesses and migrate the header "← Dashboard" nav link in 6 more previously-zero-coverage files (tier 1 + tier 2 of the row above's deferred 13) | Done for 6 of the 13; 7 genuinely distinct-domain files deliberately deferred to their own cycle | Continuing from the row above's remaining 13 files, tiered by risk rather than treated as one unit: 5 files (`SWOTEditor`, `MatrixEditor`, `StakeholderEditor`, `RiskMatrixEditor`, `RACIEditor`) call the identical load/save/layout backend trio already proven in five prior migrations, so their harnesses were mechanical; a shared canvas-stats editor shell needed its own analysis since none of its 8 consumers had ever been tested; the remaining 7 files are distinct backend surfaces, each needing its own mock design, and were deferred to a future cycle rather than rushed. One component's own refresh logic calls only the save endpoint, not the layout endpoint that every sibling file's test gates on — reading the component (rather than assuming it matched the pattern) avoided a test that would have hung waiting on a call that never happens. The thinnest of the shared shell's 8 consumers (72 lines, no own mount logic or side effects) was chosen as its test's mount point; that test's layout-fetch mock deliberately returns no data, so the shell's chart-rendering child component (which needs a real canvas, unavailable in the test environment) never renders and doesn't add noise. 6 new test files (all previously zero coverage), one test each pinning the migrated button's class string. Fault-seeded twice, independently: once on a tier-1 file's button (confirmed only that file's test fails), once on the shared shell's button, switched to a different exempt variant rather than a simple typo, to prove the test discriminates between two visually similar no-padding variants — the same risk class the shell's earlier fault-seed covered when it was first migrated. A completeness grep confirmed exactly 7 occurrences remain in exactly the 7 deferred files. Full suite: 303/303 passing, `svelte-check`/`eslint` clean. Found in passing, not migrated: all 6 touched files also have a raw load-error "Back to dashboard" button matching the pattern already migrated elsewhere — flagged as next work (closed in the row below). Not done this cycle: the 7 remaining distinct-backend files. |
| Migrate the load-error "Back to dashboard" button found in passing across the row above's 6 files | Done — 9 sites, 9 files (6 planned + 3 more found via a completeness grep this same cycle) | Continuing from the row above's flagged next work. All 6 files touched last cycle had an identical, already-established load-error button pattern, migrated the same way, with one new test per file forcing the load call to reject. Fault-seeded once rather than per file, since the pattern itself was already proven safe across four prior migrations: changed one file's variant and confirmed only its own new test failed, not any sibling's. Full suite: 309/309 passing. A follow-up review of this pass caught that the completeness check had been tracking the wrong class pattern — the nav-link string, not this cycle's load-error string. Re-running the correct grep surfaced 3 more unmigrated sites in already-tested files (`FishboneEditor`, the shared canvas shell via `NetworkEditor`, `GanttEditor`), apparently missed by the earlier migration passes. Migrated all 3 the same way, with one more independent fault-seed on `GanttEditor` (confirmed only that file's own test failed, not any sibling's). Also found and corrected a stale coverage-ledger entry for `FishboneEditor` that had never been updated after an earlier test was added. Re-running both completeness greps afterward confirmed the nav-link grep is unaffected (still 7 remaining) and the load-error grep now returns zero sites app-wide — that pattern is fully closed. Full suite: 312/312 passing, `svelte-check`/`eslint` clean. |
| Resolve `CPMEditor`'s "Export PDF/A" padding-mismatch exclusion (Priority #2 continuation) | Done | Continuing from the row above's remaining item — the one `CPMEditor` button excluded from the `Button` migration over a padding mismatch. Four options were laid out for the user rather than decided unilaterally: leave it permanently excluded (matching this project's own precedent, since a new `Button` size has so far only been justified by double-digit call-site counts and this button has exactly one); round up to the existing `compact` size (a real, small visual padding change); add a new one-off size for a single call site; or force the old padding via a class override (rejected, since Tailwind resolves same-property class conflicts by stylesheet position, not class order — the same reason `Input`/`Select` don't support this pattern). The user chose to round up to `compact`, explicitly told this was a real visual change, not a pure refactor. Migrated the button, which also incidentally changes its text color from an explicit shade to the variant's inherited default — a second small visual delta, disclosed alongside the first. Since this isn't a pure refactor, it was visually verified: a throwaway static HTML page using the literal computed values from the app's real compiled CSS output rendered old vs. new side by side and was screenshotted, confirming both deltas are real but imperceptible at normal size, matching what the user was told before deciding. The throwaway harness was deleted after use and never entered the repo. One new test (the button only renders after a completed calculation run, so the test triggers that first), fault-seeded. Full suite: 313/313 passing, `svelte-check`/`eslint` clean. This closes `CPMEditor`'s entire raw-button migration — no remaining excluded call sites in that file. |
| Migrate the header nav link in the remaining 7 tier-3 files, closing the nav-link migration app-wide (Priority #2 continuation) | Done | Continuing from the row above's remaining 7 distinct-backend files (`Backlog`, `ScenarioChartEditor`, `TimelineView`, `SigmaWorkspace`, `SigmaProjectView`, `ReportComposer`, `CharterEditor`). Confirmed each file's header nav button renders outside any loading gate, so tests only needed to wait for a clean mount rather than a fully-loaded state — with a check that a silently-swallowed load failure (several of these components catch load errors internally rather than throwing) doesn't produce a false pass. Migrated all 7 nav links; `SigmaProjectView`'s unrelated "Cancel" button in its charter form was confirmed by grep not to match this pattern and correctly left alone. 7 new test files, all previously zero coverage, one test each. Fault-seeded twice, independently: once on `CharterEditor` (which also registers autosave, covering that mount path) and once on `SigmaProjectView` (the heaviest of the 7 files, with four sequential backend calls on mount) — both confirmed the exact expected diff, restored. The other 5 files rely on these two shared seeds, disclosed as such in their own coverage-ledger rows rather than implied as independent evidence. A completeness grep after the swap found exactly 2 files still containing the pattern — `AppHeader` and `Dashboard`, both the separately-scoped underlined variant, never part of the original 28-site count. Zero plain nav-link sites remain unmigrated app-wide; the totals across all three migration passes (15 + 6 + 7 = 28) reconcile exactly with the original inventory. Full suite: 320/320 passing, `svelte-check`/`eslint` clean. This closes the plain nav-link migration in its entirety, leaving only the separately-scoped underlined variant. |
| Migrate the "underline" nav-family idiom (`AppHeader` "Sign out", `Dashboard` "Settings"/"Close project"), closing the nav-family idiom app-wide (Priority #2 continuation) | Done | Continuing from the row above's last open item — the 3-site underlined variant on `AppHeader`'s "Sign out" and `Dashboard`'s "Settings"/"Close project." This was a real design decision, surfaced to the user rather than decided unilaterally: by this project's own established threshold (13/21/28 call sites justified the three prior new variants), 3 sites don't justify a fourth. Instead, an `underline` class is passed through the existing `nav` variant — safe because `underline` only sets the text-decoration property, which nothing in `nav`'s own class list touches, confirmed by inspection rather than assumed. The `Button.svelte` code comment describing these 3 sites as "deliberately left unmigrated" was updated, since that claim would now mislead a future reader. Migrated all 3 sites (the "Sign out" button's actual logout logic was deliberately not exercised by the new test, since it routes through the app's own navigation-guard machinery, tested elsewhere). 3 new tests, one fault seed (dropped the `underline` class from one site, confirmed the exact one-token diff, restored) — proportionate to the single shared mechanism this change relies on. A completeness grep after the swap returns zero files containing the old pattern. The entire nav-family idiom — 31 call sites across 28 files in total — is now closed app-wide; no fourth `Button` variant is anticipated. `Dashboard.svelte` still has roughly 20 other raw buttons untouched by this or any prior cycle — this closes only the nav-family idiom in that file, not its broader migration status. Full suite: 323/323 passing, `svelte-check`/`eslint` clean (3 pre-existing lint warnings in one test file, confirmed unrelated to this change). |
| Close the `variant="remove"` reachability gap in `ActivityEditor`/`WorkflowEditor`/`CPMEditor`; fix a real accessible-name gap found along the way | Done | 2026-08-25. The 2026-08-19 Button migration (row above's earlier sibling in this table) had left several `variant="remove"` call sites — `ActivityEditor`/`WorkflowEditor`'s "Delete edge," `CPMEditor`'s "Remove assignment" — with their class verified only by reading `Button.svelte`'s `remove` branch, not by rendering and clicking, since reaching them was judged disproportionate setup at the time (canvas node-selection). Re-examined this cycle per an adversarial-advisor prompt: "check whether jsdom can reach these at all — the headers say disproportionate, not impossible." It can. For `ActivityEditor`/`WorkflowEditor`'s "Delete edge," seeding the chart pre-loaded with two nodes and one edge (via a custom `GetChart`/`LayoutChart` mock) makes the first node's canvas element clickable and selects it directly — no "+ Node"/"Connect…" flow needed. For `CPMEditor`'s "Remove assignment," the shared `_layered_editor_shell.svelte`'s own `+ Node` button sets `selectedId` synchronously on click, rendering `CPMEditor`'s `nodeDetailPanel` snippet immediately; its own "+ Assign resource" button then surfaces the target — no `LayeredDiagram` canvas interaction needed at all, a shorter path than the one originally assumed.<br><br>A completeness grep for `variant="remove"` across the three files (prompted by a second adversarial-review pass, to avoid overclaiming "gap closed" against an undercounted site list) found a fourth, previously untested site missed by the original scoping: `ActivityEditor`'s "Remove swimlane," reachable even more simply — the already-tested "+ Swimlane" button alone renders it, no node selection needed. Unlike "Delete edge"/"Remove assignment," this one already carried both `aria-label` and `title`, so no accessibility fix was needed there.<br><br>Writing `CPMEditor`'s test surfaced a real, independent accessibility defect, not a test-coverage gap: "Remove assignment" had `title="Remove assignment"` but no `aria-label`. Its visible content is the bare glyph `✕`; per the accessible-name-computation order, `title` is only used when there is no other name source, so the button's actual computed accessible name was "✕" (or whatever an AT reads for that glyph), not "Remove assignment" — a materially worse accessible name than its "Delete edge"/"Remove swimlane" siblings, which have always carried both attributes. Fixed with a one-line `aria-label="Remove assignment"` addition. Confirmed load-bearing by fault-seeding: reverting the fix causes `getByRole('button', { name: 'Remove assignment' })` to throw (multiple/zero-match) rather than silently pass. 4 new tests total (one per `variant="remove"` site: two in `ActivityEditor`, one each in `WorkflowEditor`/`CPMEditor`), each independently verified pass/fail against its own fault seed. Baseline test count (357) confirmed by measurement (`git stash` + a clean run), not derived by subtraction, before claiming the delta. Full suite: 361/361 passing (up from 357 — 4 net new, matching the 4 new tests, since none were removed), `svelte-check` clean (260 files, 0 warnings), production build clean. No Go files touched. |
| Close the native-window half of the `Tabs.svelte` keyboard-verification gap (`docs/design/accessibility-remaining-work-plan.md` M1) | Done | 2026-08-25, later the same day as the row above. A prior pass had closed the browser-tab half of this gap (real trusted keys, real `:focus-visible` rendering, but through a Chromium tab hitting the `wails dev` asset server, not the native desktop window). A same-session computer-use request to control `gopmgr.app` was denied at the approval prompt; the user then explicitly re-granted it. With access confirmed, the actual launched native window (real macOS title bar, no browser involved) was driven directly: signed in, opened the persisted test project, and exercised both `Tabs.svelte` consumers — Project Settings' 5 tabs and Dashboard's 4 — with real hardware ArrowRight/ArrowLeft (both-direction wraparound), Home, End, and Tab, screenshotting and zooming after each keypress to visually confirm the correct tab activates and the app's own focus-ring CSS actually renders, and that Tab exits the tablist into its panel rather than cycling. Zero defects in either consumer.<br><br>One real regression risk was investigated and ruled out along the way: navigating to "Dashboard" showed no visible tab row at first, which could have meant the tablist had silently stopped rendering on the app's most-visited screen. Traced rather than assumed: grepped the routing (`Portfolio.svelte`'s project-card click and the in-app "← Dashboard" link both call `goto('dashboard')`, which resolves to `Dashboard.svelte` per `App.svelte`'s route table) and confirmed `Dashboard.svelte:494` still renders `<Tabs>` unchanged. The tab row was simply further down the page than expected — the "always-visible header meta-card strip" above it (Stakeholders/Suppliers & items/Timeline nav cards, Budget, and the much-expanded Cost Control ledger built in recent cycles) had grown long enough to push it below the fold. No code change; confirmed by scrolling.<br><br>**Operational note for future sessions**: `open_application("gopmgr")` is the wrong way to reach this app — it killed the long-running isolated dev instance and tried to launch a second, non-isolated one that immediately crashed (`unable to infer the AssetDir... 'frontend/dist' does not exist`, since this app runs in Wails dev mode, not from a static build). The correct path is `preview_start` with the `wails-isolated` launch config (`.claude/launch.json`), which relaunches the isolated `wails dev` process and its native window together; recovered this way mid-session with no data loss (the isolated env's persisted SQLite data survived the process restart). No Go or frontend files changed this row — verification only. |
| Independently measure the action-button (500–800) contrast shades the June light-theme remap left "reasoned but not measured"; fix every genuine AA failure found | Done — all 12 real failing sites fixed, user chose to darken the shade rather than restrict to non-text use | 2026-08-25. `docs/design/accessibility-remaining-work-plan.md` §4 named this as the next self-contained item; `session-notes.md:810`'s original claim ("shades 500–800 have sufficient contrast and would regress if lightened") was never actually measured. It was measured this cycle against real, build-verified values — not memorized Tailwind v3 hex, since this app pins Tailwind **v4.3.3**: ran a real `npm run build` and read the generated `--color-{red,emerald,amber}-{500,600,700,800}` custom properties directly out of `dist/assets/*.css`, then applied the textbook WCAG 2.1 SC 1.4.3 formula (sRGB-gamma-linearized relative luminance, `(L_lighter+0.05)/(L_darker+0.05)`, AA normal-text threshold 4.5:1) against every real call site found by grepping `bg-{red,emerald,amber}-[5678]00` across `frontend/src`.<br><br>**Scope correction**: the plan's "six action-button shades (500–800) across red/emerald/amber/orange/rose/sky" overstated real usage — orange/rose/sky have zero real call sites at 500–800 (only their existing, already-remapped 300/950 badge shades are used), and red has no real `800` usage either. Real scope is red (500–700), emerald (500–800), amber (500–800) only.<br><br>**Findings, ranked by severity, three buckets** (resolved same day — see "Resolution" below; full file:line table kept in `docs/design/accessibility-remaining-work-plan.md` §3): **Bucket A — fixed this cycle.** 3 sites (`CharterEditor.svelte:265,282`, `Dashboard.svelte:564`) use `bg-emerald-800`/`bg-amber-800` with no explicit text color, relying on the app's ambient `text-slate-200`, whose value inverts between themes (`app.css`: `#e2e8f0` dark, `#1e293b` light). These pass in dark theme (6.25:1 / 5.78:1) but drop to 1.90:1 / 2.05:1 in light theme — a real, previously-unverified defect (the button's label was never intended to go near-illegible in light mode), not a design tradeoff. Fixed by adding an explicit `text-white` to all 3, confirmed by computation to pass in both themes and both rest/hover states (emerald-800+white 7.71:1, amber-800+white 7.13:1, and their hover destinations emerald-700/amber-700+white 5.43:1/5.05:1 — all previously-failing ambient-hover states, now fixed as a side effect). **Bucket B — not fixed, partial-fix-only case.** `SprintList.svelte:328` (`bg-emerald-700 hover:bg-emerald-600`, ambient text) fails at rest in both themes (4.40:1 dark, 2.70:1 light) — `text-white` would fix the rest state (5.43:1) but its hover state (`emerald-600`) would still fail even with white text (3.73:1), so the Bucket-A one-line fix doesn't fully close this site; left open pending a hover-shade decision rather than applying a half-fix silently. **Bucket C — not fixed, needs a real shade/design decision.** Fails regardless of text-color choice: `bg-emerald-600` + white text, 3.73:1 rest / 2.50:1 hover (`SignCertificateModal.svelte:254`, `CharterEditor.svelte:326`, `SigmaProjectView.svelte:265`); `bg-emerald-600` ambient, fails both themes at rest (`SigmaProjectView.svelte:532`); `bg-emerald-600` + white text with **no hover variant** — a DMAIC current-phase indicator's permanent resting state, always 3.73:1 (`SigmaPhaseStepper.svelte:55`); `bg-amber-600` + white text, 3.20:1 rest / 2.15:1 hover (`SigmaFishbone.svelte:174`); `Button.svelte:78`'s shared `caution` variant's hover state (`amber-600`, 3.20:1 — rest `amber-700` passes at 5.05:1) cascading to every `variant="caution"` button app-wide (`AppSettings.svelte:383`, `AdminPanel.svelte:338`); `bg-red-500` hover on `ProjectPicker.svelte:199` (3.81:1 — rest `red-600` passes at 4.87:1). None of these pass 4.5:1 at any text-color choice tried, so remediation means either darkening the shade (a real brand-color change, cascading through the shared `Button.svelte` component for the `caution` case) or restricting these shades to non-text/decorative uses, which `session-notes.md:810`'s original author explicitly worried would visually regress. Left for the user to choose, per this project's own precedent (see the "Export PDF/A" row above) of surfacing a visible design change rather than deciding it unilaterally.<br><br>**Excluded from this pass, different pattern**: `bg-{red,emerald,amber}-500` progress-bar/stepper fills with no text overlay (`BudgetPanel.svelte:100`, `DORADashboard.svelte:119-122`, `SigmaPhaseStepper.svelte:77`) are non-text UI components (WCAG SC 1.4.11, 3:1 threshold, not SC 1.4.3) — not measured here; `Portfolio.svelte:66,68,70`'s `/20`-opacity badge tints and `ProjectPicker.svelte:222`'s `hover:bg-red-600/80` translucent overlay both need alpha-compositing math this pass didn't do — left as still-unverified, matching the plan's existing P3 "SWOT tint" disclosure.<br><br>**Methodology note**: ratios are computed against the sRGB hex fallback Tailwind v4 emits for standard-gamut displays; Tailwind v4 also emits a more-saturated `lab(...)` value for wide-gamut (P3) displays, which a browser on such hardware uses instead and would compute a different ratio — this measurement's scope is standard sRGB displays only. Verification: `svelte-check --fail-on-warnings` clean (260 files), `vitest run` 361/361 passing (unchanged — no test asserts these class strings), production build clean. No native-window visual re-verification this cycle: the isolated `gopmgr.app`/`wails dev` pair was already running, but the host Mac was screen-locked when computer-use access was granted, and a browser-tab-mode attempt hit a pre-existing `wails/ipc.js` null-dereference on project-open unrelated to this change — for a plain `text-white` class addition, Tailwind's CSS output is deterministic, so the build-verified sRGB computation above is treated as sufficient evidence rather than re-derived visually.<br><br>**Resolution, same day**: the user chose to darken the shade rather than restrict Bucket B/C colors to non-text use. Every failing rest/hover pair was shifted up one Tailwind tier where that tier's own pairing passes both states — `emerald-600/500 → 800/700` and `amber-600/500 → 800/700` (since `700`+white passes but `600`+white fails, the pair needs the 800/700 tier, not a single-step 700/600 shift, for both rest and hover to clear 4.5:1); `red-600/500 → 700/600` (red only needs one tier, since `700`/`600` already both individually pass). Fixed: `SignCertificateModal.svelte:254`, `CharterEditor.svelte:326`, `SigmaProjectView.svelte:265,532` (the last also gained an explicit `text-white`, since it was ambient), `SprintList.svelte:328` (closing Bucket B — same emerald-800/700 + `text-white` treatment as Bucket A), `SigmaPhaseStepper.svelte:55` (single-value, no hover, one tier to `emerald-700` sufficient), `SigmaFishbone.svelte:174`, `ProjectPicker.svelte:199`, and `Button.svelte:78`'s shared `caution` variant (`amber-700/600 → 800/700`) — the last cascades to every `variant="caution"` call site without touching those files, but `AppSettings.svelte:383`/`AdminPanel.svelte:338` have their own independent inline class strings (not wired to the shared component) and were fixed separately. A completeness re-grep after the initial fixes caught one site missed by the original write-up: `ReportComposer.svelte:226` (`bg-emerald-700 hover:bg-emerald-600 text-white`) had been listed as "passing" because its rest state alone passes (5.43:1) — its hover state (`emerald-600`+white, 3.73:1) was never separately checked and fails, the identical rest-passes/hover-fails shape as the `caution`-variant bug. Fixed the same way. `Button.test.ts`'s `caution`-variant assertion (`toContain('bg-amber-700')`) was found to still pass after the shade change for the wrong reason — `bg-amber-700` is a substring of the new `hover:bg-amber-700` — so it was rewritten to assert `bg-amber-800` and `hover:bg-amber-700` as distinct tokens and fault-seeded (reverted the variant, confirmed the rewritten test fails, restored). A second adversarial-review pass on this fix, prompted before committing, flagged that `SigmaPhaseStepper.svelte:55`'s stated rationale ("no hover state, so one tier suffices") read as a general rule rather than a one-off measured fact, and pointed at its untouched, structurally identical sibling two lines below: the `isCurrent` branch's `bg-cyan-600 text-white ring-4 ring-cyan-900`. That one hadn't been in scope (cyan is a theme-remapped color via `--cyan-*` CSS variables, not a raw Tailwind red/emerald/amber shade, so the original grep never surfaced it) — but measuring it anyway found a real, independently-discovered failure: `cyan-600` (`rgb(8,145,178)`, identical in both themes — cyan is not theme-inverted like red/emerald/amber/slate) + white is 3.68:1, worse than the completed-phase state this row already fixed, and on the *current*-phase indicator, arguably the more important of the two states in a stepper UI. Fixed by bumping to `cyan-700` (`rgb(14,116,144)`, the same value already used as the app's light-theme focus-ring accent), which clears 5.36:1 — theme-independent, since cyan's CSS-variable values are identical in both `:root` and `:root[data-theme='light']` blocks in `app.css`. Verification: `svelte-check --fail-on-warnings` clean (260 files), `vitest run` 361/361 passing (unchanged count — one assertion rewritten, none added or removed), production build clean. 13 files touched. |

## P2: follow after beta stability

- Project Cost Control ledger scope: [the current project-local boundary](design/project-cost-ledger-scope.md)
  defines the ordinary product. Keep legacy Budget as separate context, then
  track Cost Control expenses, inputs, materials, and direct/indirect overhead
  as exact project-currency entries and summaries. Do not introduce portfolio
  persistence, FX, financial RBAC, transaction or statutory-accounting
  semantics, or Phase 2 reserve/forecast behavior merely to improve the ledger.
- Cost Control Phase 2: the proposed [lifecycle and authority contract](design/cost-control-phase-2-lifecycle.md)
  now gates implementation. Approve its reserve-balance meaning, contingency
  allocation equation, baseline/evidence bindings, project financial roles,
  correction/idempotency rules, and rebaseline behavior before code. Then add
  contingency-reserve movements linked to known risks/change evidence,
  time-phased cash flow/EAC reporting, and a real authorization model.
  Immutable Cost Control baseline snapshots and local-account approval
  checkpoints are available, but are not RBAC; management-reserve release
  remains blocked until a real authorization model exists. Phase 1 deliberately
  keeps the project-local ledger separate from the legacy stakeholder/agile
  budget rollup and schedule EVM, has no FX conversion, and does not treat a
  reserve balance as a commitment or actual cost.
- Governed presentation-only FX for Portfolio Analytics: [the proposed bounded
  contract](design/governed-presentation-only-fx.md) preserves the existing
  mixed-currency refusal and proposes a separate direct-pair report workflow.
  It does not authorize code, a portfolio database, FX tables, RBAC, provider
  integration, or Cost Control Phase 2 work. The precise next action is FX-0:
  explicitly approve or amend its bounded contract and record the accepted
  revision. Only then may FX-1 implement exact rate parsing and immutable
  user-private rate-set storage.
- Cost Control currency-exponent policy: JPY is now contained as a legacy-only
  case. Existing JPY projects are readable but Cost Control is read-only; new
  JPY projects and currency transitions to JPY are rejected. No value is
  converted or rescaled. Before introducing exponent-aware JPY, define
  project-scoped Cost Control scale metadata, immutable-baseline scale capture,
  a migration that defaults existing values to their fixed two-decimal
  convention, exact parsing/formatting limits, and the relationship to legacy
  Budget, schedule EVM, stakeholder rollups, analytics, reporting, and future
  FX. USD, EUR, GBP, CAD, AUD, and CHF retain the current two-decimal boundary.
- Cost Control allocation and forecast semantics: define allocation, reserve
  drawdown, burn, and forecast policy before adding a remaining-spend metric.
  Phase 1 intentionally presents the legacy Budget rollup only as separate
  context and does not derive a remaining or unallocated amount.
- Encryption migration durability: the canonical path now remains present on
  synchronous preparation/publication failure through a single replacement
  operation. Add native Windows execution evidence and a power-loss durability
  strategy/test before claiming stronger crash-consistency guarantees.
- Penpot evidence bridge: the local self-hosted stack was upgraded to 2.17.1,
  the exposed bridge credential was revoked and replaced outside the
  repository, and Codex completed the canonical read-only probe before and
  after a controlled MCP-container restart. The saved board `Cost Control —
  Penpot Recovery Evidence (2026-08-21)` records the verified scope. Start a
  fresh Claude Code client and repeat its read-only probe before treating the
  Claude bridge as live-verified; do not add credentials, configuration files,
  or private recovery material to the repository.
- Restore a usable normal window frame across relaunch.
- Improve local project discovery without weakening data isolation — partly
  done 2026-08-18: `enumerateProjects` (`app_projects.go`) only ever did a
  single-level directory read, so a project subfolder wrapped in one extra
  directory (manual reorganization, archive extraction, backup restore) was
  silently invisible with no error. Now discovers one extra level of
  wrapping via a depth-bounded `filepath.WalkDir`, capped so it can't become
  an unbounded scan, and explicitly never follows a symlinked file or
  descends into a symlinked directory — closing the "without weakening data
  isolation" half by construction rather than by omission. 4 new tests in
  `project_storage_test.go` (nested discovery, the depth-cap boundary, and
  both symlink cases), each fault-seeded to confirm it's load-bearing.
  Remaining, not attempted this cycle: any UI-level "browse for a project
  file elsewhere on disk" affordance, which is a materially different
  (frontend + Wails-binding) feature, not a backend discovery-depth fix.
- Expand release validation only after it has an owner, target, and repeatable
  acceptance criteria.
- Done 2026-08-20, corrected 2026-08-24: Timeline aggregation merges Charter
  milestones with scheduled Gantt/CPM task milestones. `timelineMilestones()`
  reuses the calendar-aware chart layout shared with `App.LayoutChart`,
  includes explicit and zero-duration milestones, uses the computed
  completion date, and omits unanchored chart tasks rather than treating CPM
  offsets as dates. **The 2026-08-20 entry overclaimed the Charter half**:
  `projectMilestones()` queried the document kind `"charter"`, which no real
  document has ever had (Project Charter is registered as
  `charter_word`/`charter_excel`) — so real Charter documents never actually
  surfaced milestones on the Timeline or in `.ics` export, despite the
  passing test suite, which exercised the bug's own fixture (`Kind:
  "charter"` written directly via `db.SaveDocument`, bypassing the
  registry validation every real document-creation path enforces). The
  CPM/Gantt half was always correct. Fixed 2026-08-24 in `app_foundation.go`;
  see `TEST_COVERAGE_LEDGER.md`'s `timeline_move_test.go` row for the full
  account and the added regression test.
- Done 2026-08-24: Timeline milestones now visually distinguish their two
  sources. Both `timeline.Entry` and `timeline.Milestone` gained a
  `MilestoneSource` field (`"charter"`/`"chart"`); `TimelineView.svelte`
  renders a Charter-sourced milestone as a purple circle and a chart-sourced
  one as a pink diamond, paired with a `milestone · charter`/`milestone ·
  chart` text label (not color alone, per WCAG 1.4.1). Closes the
  "identical purple regardless of source" gap the row above's fix had
  explicitly left open. Live-verified end-to-end in a running isolated
  instance: a real Charter document and a real Gantt-chart milestone
  rendered with the correct distinct color/shape/label. The gap this left
  open — `ExportProjectICS` built its iCal events field-by-field from
  `timeline.Entry` without reading `MilestoneSource`, so the `.ics` export
  still emitted `CATEGORIES:MILESTONE` for both sources undifferentiated —
  was closed the same day: `ExportProjectICS` now emits
  `CATEGORIES:MILESTONE_CHARTER`/`CATEGORIES:MILESTONE_CHART`, matching the
  Timeline view's own distinction. `internal/export/ical.go`'s `Category`
  field comment was corrected to match (it previously described a coarser,
  now-stale enumeration). See `TEST_COVERAGE_LEDGER.md`'s
  `timeline_move_test.go` and `TimelineView.test.ts` rows for full test
  evidence and fault-seeds.
- Done 2026-08-20: completed the documentation audit of `tools/`,
  `docs/design/`, `ROADMAP.md`, `VISION.md`, `code-map/recent-decisions.md`,
  `debian/`, `.github/workflows/*.yml`, and `wails.json`. Corrected stale
  chart-catalog counts and Project Settings status in the Dashboard proposal,
  aligned Debian's Go build dependency with `go.mod`, and refreshed the
  installed man-page date; the other audited claims matched current source or
  intentionally preserved historical release records.
- Done 2026-08-20: re-verified the XLSX formula-injection boundary against
  the pinned `excelize` v2.11.0. `TestRenderXLSXStoresFormulaLikeTitlesAsText`
  proves formula-like task titles remain string cells; the source comment now
  points to that test.

## Maintenance rules

- Use `Planned`, `In progress`, `Blocked`, `Done`, or `Deferred`.
- A completed item must link to current tests or an archived release artifact;
  source inspection alone is insufficient for native behavior.
- Keep this file concise. Put durable architecture decisions in an ADR and
  point-in-time investigation details in Git history.
