<!--
SPDX-FileCopyrightText: 2026 James L. Burns and The GoPMgr Contributors
SPDX-License-Identifier: GFDL-1.3-or-later
-->

# Beta release backlog

This is the release-readiness backlog for the current feature set. It is not a
feature wish list. New features should wait unless they fix security, data
loss, or a release blocker.

## Release exit criteria

- Every P0 item is complete.
- No known critical security, data-loss, first-run, or installer defect is
  open for an advertised platform.
- `make verify`, `make release-scope`, `make license-check`, and
  `make check-release` pass from the release commit.
- Release notes describe every remaining limitation without implying unverified
  platform support or trusted signing.
- Installation, upgrade, and data-preserving removal have current evidence for
  each advertised package format.

## P0: must complete before wider beta distribution

| Item | Status | Completion evidence |
| --- | --- | --- |
| Prevent silent editor data loss | In progress — scoped to darwin/arm64, unsigned local build | The shared guard protects registered editors on navigation, project close, sign-out, and window close; failed saves and edits made during a save remain visible and block unsafe continuation. The 2026-08-15 native-close guard freezes renderer interaction after a clean, saved, or discarded decision, then has Go grant and consume its one-shot permit while invoking Wails quit; `main_test.go` and `frontend/src/lib/native-close.test.ts` cover the source contract. `WorkItemEditor`, `SprintList`, and `StakeholderManager` each register only their current open draft, with timed auto-save disabled because a successful modal save closes it. In all three, a late modal-field edit survives the first save, retains backend-owned fields, and requires another successful save before close. Stakeholder saves preserve exact money minor units while they are safe JavaScript integers unless the matching major-unit field changed; an unsafe Wails number fails closed rather than being saved rounded. 2026-08-16 (first cycle): installed the packaged `.pkg` build to `/Applications` and launched it directly with an isolated `XDG_DATA_HOME`, confirming empirically (not just by source inspection) that the compiled binary creates and uses the override data root and never touches the real one. The GUI-driven portion was attempted but blocked that cycle: computer-use GUI access could not be approved in that execution context (a "scheduled run," per the tool's own error). 2026-08-16 (second cycle, interactive session): GUI access was granted. Drove the packaged `.pkg` through account/project/`StakeholderManager`-draft/native-red-button-close and confirmed, by direct observation in the compiled binary: (1) the guard dialog fires on real native close, not just in unit tests; (2) Cancel keeps the window open and the field's edit intact; (3) a Save performed *after* a refused close succeeds and the record appears in the list; (4) Discard quits cleanly, and a relaunch against the same isolated data root confirms the discarded draft was genuinely never persisted (not inferred). **Verified scope: the window-close (red traffic-light) trigger on `StakeholderManager`, darwin/arm64, unsigned local build.** Cmd+Q, sign-out, project-close/navigation-away, and the `WorkItemEditor`/`SprintList` editors remain source-level only, not GUI-exercised. See `.agent_memory/packaged-app-lifecycle-evidence-2026-08-16.md` (Stage C) for the full sequence, the mid-cycle advisor gaps that were closed before this write-up, and a flagged (unconfirmed) side observation about `StakeholderManager`'s own Cancel/×/Escape controls not dismissing a dirty draft. |
| Validate package lifecycles | In progress — scoped to darwin/arm64 `.dmg`/`.pkg`, unsigned local build | 2026-08-16: built both macOS formats (`make package-macos`, `make package-macos-installer`) and inspected them read-only — `pkgutil --expand-full` on the `.pkg` confirmed no `Scripts`/postinstall logic and, via `lsbom`, that every payload path is under `./gopmgr.app/...` with nothing written outside the app bundle (structural proof install/removal cannot touch user data at `~/Library/Application Support/GoPMgr`, a disjoint path); `hdiutil attach -readonly` on the `.dmg` confirmed the standard drag-to-Applications layout. Installed the `.pkg` payload to `/Applications` (manual copy, justified as equivalent to `installer -pkg -target /` given the confirmed absence of Scripts) and did a real isolated first-run launch — data root created correctly, real user data untouched, clean process teardown, clean removal of the installed bundle with real user data confirmed intact afterward. 2026-08-16 (second cycle): the GUI native-close testing (see the row above) also relaunched the installed `.pkg` binary a second time against the same isolated `XDG_DATA_HOME` and confirmed the account and previously-saved project data survived the relaunch (second-run/data-durability evidence, distinct from first-run). **Not tested this cycle:** upgrade and reinstall (no receipt-tracked `installer -pkg` run — would need root, not requested/approved), and Linux/Windows package formats (no host available). Real receipted removal (`pkgutil --forget`/Finder trash) also not exercised, only a file-level equivalent. See `.agent_memory/packaged-app-lifecycle-evidence-2026-08-16.md`. |
| Confirm `Login` cannot leave a stale session's project open under a new user's key | Investigated, closed — see `app_session_test.go` | Spotted 2026-08-16 while enumerating every write site of `a.dek` (`app_session.go`) for an unrelated investigation: `App.Login` set `a.user` and `a.dek` to the newly-authenticated user's values but never checked whether `a.db`/`a.dbPath` were already set from a previous session — unlike `Logout` and `shutdown`, which always clear `a.db` and `a.dek` together in the same locked section. Investigated the frontend's login flow before deciding on a fix: the only transition into the login screen reachable from a post-login, project-open state is `AppHeader`'s sign-out button, which already calls `window.go.main.App.Logout()` (closing `a.db`) before switching views; the other two transitions into that flow (`recovery_reset`, `create_account`) are only reachable from the login screen itself, where `a.db` is already nil — confirmed by tracing every `goto`/`requestNavigation` call site into those three views. **Not a live defect through the shipped UI.** Closed anyway with a defense-in-depth guard in `Login` (`app_session.go`), checked once, in the same critical section as the assignment it guards — deliberately *after* credentials are verified and a DEK is derived, not before, since an earlier-checking design left a TOCTOU window for a concurrent `OpenProject` to set `a.db` during `Authenticate`/`UnlockDEK`'s real password-hashing work. The DEK derived on a refusal is zeroed immediately (ADR-001); a refused login also still stamps the account's `last_login` (`Authenticate` writes it before this guard runs), a known and accepted side effect since avoiding it would mean reopening the race — independent of frontend behavior either way, and cheap enough that relying solely on frontend discipline for a cross-user key-isolation invariant wasn't worth the residual risk. `TestLoginRefusesWhileProjectOpen` (`app_session_test.go`) proves the refusal and that the original session's user and live database connection are completely untouched by the attempt. |
| Confirm self-heal is reachable on real encrypted corruption | Investigated, closed — see `repair_and_swap_test.go` | `App.RepairAndSwap()` (`app_documents.go`) requires `a.db != nil`, which requires `OpenProject` to have already succeeded — every project is SQLCipher-encrypted. A single-offset probe on 2026-08-16 initially found `OpenProject` itself failing on the exact light-corruption fixture `internal/db/repair_selfheal_test.go`'s `corruptLightly` uses (byte offset 4097), raising the concern that `RepairAndSwap` might be unreachable exactly when needed. Resolved via a systematic page-by-page sweep (one byte flipped per 4096-byte page across a 105-page encrypted fixture, `OpenProject`/a real query/`RepairAndSwap` outcome recorded per page, byte restored before the next trial — reproducible, confirmed identical across repeated runs): `OpenProject` succeeded after single-page corruption on 80/105 pages (76%) — the earlier offset-4097 failure was one unlucky page (page 1, hit by `Migrate`'s schema/settings bootstrap), not evidence of a systemic problem. The sweep varied exactly one dimension — a single byte flipped in one page at a time — and did not cover multi-page corruption, torn/partial writes, or truncation, so this closure is scoped to single-page corruption specifically, not corruption in general. **Within that scope, `RepairAndSwap` is reachable in the large majority of cases** — `TestRepairAndSwapHealsReachableLightCorruption` (`repair_and_swap_test.go`) pins one such case end-to-end: `OpenProject` succeeds, a real query succeeds, then `RepairAndSwap` still detects the underlying corruption during its own full-database `VACUUM INTO` pass and heals it. **Residual finding, not a defect:** of the 3 pages (of 105) where corruption was severe enough that a real query failed even after `OpenProject` succeeded — genuinely user-visible corruption, exactly `RepairAndSwap`'s target scenario — `RepairAndSwap`'s own healing attempt also failed on all 3 in this sample, rather than producing a healthy copy. `TestRepairAndSwapCanFailToHealEvenWhenReached` pins this as current, real behavior (not asserted as a bug — no test can require `VACUUM INTO` to heal arbitrary corruption). This mirrors the same light-vs-severe corruption distinction `internal/db/repair_selfheal_test.go` already documents for plaintext SQLite (`corruptLightly` heals, `corruptSeverely` doesn't); this sweep confirms the same distinction holds on the encrypted path, with a small (n=3) sample suggesting user-visible corruption skews toward the unhealable side more than the overall 76%/24% split would suggest — worth a larger sample in a future cycle if this becomes a real support-burden signal, but not blocking: no code behavior change is needed to close this item, since `RepairAndSwap` was already doing the right thing when reachable. |

## P1: release trust and usability

| Item | Status | Completion evidence |
| --- | --- | --- |
| Sign and notarize distributables where supported | Blocked | Release credentials are configured and the published artifacts pass the platform trust checks. |
| Configure the signed update channel | In progress | A controlled prerelease-to-prerelease update verifies signature, channel, version, URL, and digest while offline use remains unaffected. |
| Obtain trusted PAdES evidence | Blocked | A release-certificate sample validates as trusted and independent interoperability evidence is archived. |
| Complete keyboard and assistive-technology smoke coverage | Planned | First run, project editing, dialogs, reports, and native window controls are exercised with the supported assistive tools. |
| Reconcile macOS package version-string sourcing | Done for macOS (darwin); Linux deliberately not applied | Spotted 2026-08-16 while inspecting build artifacts: `scripts/package-macos.sh` and `scripts/package-macos-installer.sh` derived their filename version differently. Root cause, found on investigation, was sharper than a mismatch: both scripts' original fallback used `git describe --tags --abbrev=0`, which silently reuses the nearest tag's bare name for a checkout that is any number of commits past it — two different trees could produce identically-named local-build artifacts. Fixed by extracting the derivation into `scripts/package-version-lib.sh` (`release_version()`), used by both scripts: explicit `VERSION` env var wins (this is what CI always sets — `release.yml:208,214` — so the real release pipeline was never exposed to this bug), else `git describe --tags` *without* `--abbrev=0` so a build past its tag gets an honest `-<N>-g<sha>` suffix, else `0.0.0`. Verified by rebuilding both artifacts at the same commit: filenames now agree exactly (`...1.1.0-alpha.1-132-g9a54f40...`), and the `.pkg`'s actual `PackageInfo` `version="1.1.0"` (the upgrade-comparison key, read from the app bundle's own Info.plist, unrelated to the filename) is unchanged. `scripts/package-version-lib_test.sh` proves the fix (case 3 fails if `--abbrev=0` is reintroduced) and is wired into `make verify` and both `make package-macos*` targets. Also discovered `scripts/package-macos_test.sh` itself was invoked by nothing — not the Makefile, not CI, not `check-release.sh` — and wired it into `package-macos:` alongside the fix. **`scripts/package-linux.sh` carries the identical `--abbrev=0` one-liner and is deliberately left unfixed**: rpm `Version` fields reject hyphens, `git describe`'s non-`--abbrev=0` output contains one, and there is no Linux host in this cycle to verify `nfpm`'s actual behavior against that — applying the same fix blind would be an unverified change to a release-path script. |

## P2: follow after beta stability

- Restore a usable normal window frame across relaunch.
- Improve local project discovery without weakening data isolation.
- Expand release validation only after it has an owner, target, and repeatable
  acceptance criteria.

## Maintenance rules

- Use `Planned`, `In progress`, `Blocked`, `Done`, or `Deferred`.
- A completed item must link to current tests or an archived release artifact;
  source inspection alone is insufficient for native behavior.
- Keep this file concise. Put durable architecture decisions in an ADR and
  point-in-time investigation details in Git history.
