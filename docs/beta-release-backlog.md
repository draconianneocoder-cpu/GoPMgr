<!--
SPDX-FileCopyrightText: 2026 James L. Burns and The GoPMgr Contributors
SPDX-License-Identifier: GFDL-1.3-or-later
-->

# Beta release backlog

This is the release-readiness backlog for the current feature set. It is not a
feature wish list. New features should wait unless they fix security, data
loss, or a release blocker.

## Release exit criteria

- Every P0 item is complete.
- No known critical security, data-loss, first-run, or installer defect is
  open for an advertised platform.
- `make verify`, `make release-scope`, `make license-check`, and
  `make check-release` pass from the release commit.
- Release notes describe every remaining limitation without implying unverified
  platform support or trusted signing.
- Installation, upgrade, and data-preserving removal have current evidence for
  each advertised package format.

## P0: must complete before wider beta distribution

| Item | Status | Completion evidence |
| --- | --- | --- |
| Prevent silent editor data loss | In progress — scoped to darwin/arm64, unsigned local build | The shared guard protects registered editors on navigation, project close, sign-out, and window close; failed saves and edits made during a save remain visible and block unsafe continuation. The 2026-08-15 native-close guard freezes renderer interaction after a clean, saved, or discarded decision, then has Go grant and consume its one-shot permit while invoking Wails quit; `main_test.go` and `frontend/src/lib/native-close.test.ts` cover the source contract. `WorkItemEditor`, `SprintList`, and `StakeholderManager` each register only their current open draft, with timed auto-save disabled because a successful modal save closes it. In all three, a late modal-field edit survives the first save, retains backend-owned fields, and requires another successful save before close. Stakeholder saves preserve exact money minor units while they are safe JavaScript integers unless the matching major-unit field changed; an unsafe Wails number fails closed rather than being saved rounded. 2026-08-16 (first cycle): installed the packaged `.pkg` build to `/Applications` and launched it directly with an isolated `XDG_DATA_HOME`, confirming empirically (not just by source inspection) that the compiled binary creates and uses the override data root and never touches the real one. The GUI-driven portion was attempted but blocked that cycle: computer-use GUI access could not be approved in that execution context (a "scheduled run," per the tool's own error). 2026-08-16 (second cycle, interactive session): GUI access was granted. Drove the packaged `.pkg` through account/project/`StakeholderManager`-draft/native-red-button-close and confirmed, by direct observation in the compiled binary: (1) the guard dialog fires on real native close, not just in unit tests; (2) Cancel keeps the window open and the field's edit intact; (3) a Save performed *after* a refused close succeeds and the record appears in the list; (4) Discard quits cleanly, and a relaunch against the same isolated data root confirms the discarded draft was genuinely never persisted (not inferred). **Verified scope: the window-close (red traffic-light) trigger on `StakeholderManager`, darwin/arm64, unsigned local build.** Cmd+Q, sign-out, project-close/navigation-away, and the `WorkItemEditor`/`SprintList` editors remain source-level only, not GUI-exercised. See `.agent_memory/packaged-app-lifecycle-evidence-2026-08-16.md` (Stage C) for the full sequence, the mid-cycle advisor gaps that were closed before this write-up, and a flagged (unconfirmed) side observation about `StakeholderManager`'s own Cancel/×/Escape controls not dismissing a dirty draft. **2026-08-17: that side observation's root cause is now confirmed, not just hypothesized** — the GUI evidence never showed data loss (the guard's shared dirty registration survived every failed in-form dismissal and the native-close path still worked), so this row's own scope is unaffected, but the fix is tracked as its own P1 row ("Replace remaining `window.confirm()` dialogs with the app's own modal"). **2026-08-18 (`/go-high-assurance` concurrency pass, `main_test.go`'s ledger row has full detail)**: added two concurrency tests proving, under `go test -race` with a fault-seeded lock removal as evidence the tests are load-bearing (three distinct data races reported and confirmed gone on restore), that the native-close guard's mutex-protected one-shot permit state machine is race-free under genuinely concurrent access — the existing tests had only ever exercised it sequentially. A second test demonstrated (not hypothesized) a real TOCTOU gap where a concurrent `EnableNativeCloseGuard` call can revoke a permit `CompleteNativeClose` just granted, checked against `native-close.ts`'s actual control flow and found unreachable from the current frontend rather than assumed safe. Added a doc comment to `main.go` recording two facts traced against the vendored Wails v2.13.0 source: every quit trigger (window-close on all platforms, Cmd+Q, this file's own Quit menu item) funnels through the same dispatcher path into `beforeClose`; and `OnBeforeClose` is invoked asynchronously on darwin/linux but synchronously on Windows, bounded to only what was traced. **This narrows a different gap than the one below and does not change it**: no GUI path was exercised this cycle, on any platform. **Verified scope remains exactly as stated above: the window-close trigger on `StakeholderManager`, darwin/arm64, unsigned local build.** Cmd+Q, sign-out, project-close/navigation-away, and the `WorkItemEditor`/`SprintList` editors remain source-level only, not GUI-exercised. |
| Validate package lifecycles | In progress — scoped to darwin/arm64 `.dmg`/`.pkg`, unsigned local build | 2026-08-16: built both macOS formats (`make package-macos`, `make package-macos-installer`) and inspected them read-only — `pkgutil --expand-full` on the `.pkg` confirmed no `Scripts`/postinstall logic and, via `lsbom`, that every payload path is under `./gopmgr.app/...` with nothing written outside the app bundle (structural proof install/removal cannot touch user data at `~/Library/Application Support/GoPMgr`, a disjoint path); `hdiutil attach -readonly` on the `.dmg` confirmed the standard drag-to-Applications layout. Installed the `.pkg` payload to `/Applications` (manual copy, justified as equivalent to `installer -pkg -target /` given the confirmed absence of Scripts) and did a real isolated first-run launch — data root created correctly, real user data untouched, clean process teardown, clean removal of the installed bundle with real user data confirmed intact afterward. 2026-08-16 (second cycle): the GUI native-close testing (see the row above) also relaunched the installed `.pkg` binary a second time against the same isolated `XDG_DATA_HOME` and confirmed the account and previously-saved project data survived the relaunch (second-run/data-durability evidence, distinct from first-run). **Not tested this cycle:** upgrade and reinstall (no receipt-tracked `installer -pkg` run — would need root, not requested/approved), and Linux/Windows package formats (no host available). Real receipted removal (`pkgutil --forget`/Finder trash) also not exercised, only a file-level equivalent. See `.agent_memory/packaged-app-lifecycle-evidence-2026-08-16.md`. |
| Confirm `Login` cannot leave a stale session's project open under a new user's key | Investigated, closed — see `app_session_test.go` | Spotted 2026-08-16 while enumerating every write site of `a.dek` (`app_session.go`) for an unrelated investigation: `App.Login` set `a.user` and `a.dek` to the newly-authenticated user's values but never checked whether `a.db`/`a.dbPath` were already set from a previous session — unlike `Logout` and `shutdown`, which always clear `a.db` and `a.dek` together in the same locked section. Investigated the frontend's login flow before deciding on a fix: the only transition into the login screen reachable from a post-login, project-open state is `AppHeader`'s sign-out button, which already calls `window.go.main.App.Logout()` (closing `a.db`) before switching views; the other two transitions into that flow (`recovery_reset`, `create_account`) are only reachable from the login screen itself, where `a.db` is already nil — confirmed by tracing every `goto`/`requestNavigation` call site into those three views. **Not a live defect through the shipped UI.** Closed anyway with a defense-in-depth guard in `Login` (`app_session.go`), checked once, in the same critical section as the assignment it guards — deliberately *after* credentials are verified and a DEK is derived, not before, since an earlier-checking design left a TOCTOU window for a concurrent `OpenProject` to set `a.db` during `Authenticate`/`UnlockDEK`'s real password-hashing work. The DEK derived on a refusal is zeroed immediately (ADR-001); a refused login also still stamps the account's `last_login` (`Authenticate` writes it before this guard runs), a known and accepted side effect since avoiding it would mean reopening the race — independent of frontend behavior either way, and cheap enough that relying solely on frontend discipline for a cross-user key-isolation invariant wasn't worth the residual risk. `TestLoginRefusesWhileProjectOpen` (`app_session_test.go`) proves the refusal and that the original session's user and live database connection are completely untouched by the attempt. |
| Confirm self-heal is reachable on real encrypted corruption | Investigated, closed — see `repair_and_swap_test.go` | `App.RepairAndSwap()` (`app_documents.go`) requires `a.db != nil`, which requires `OpenProject` to have already succeeded — every project is SQLCipher-encrypted. A single-offset probe on 2026-08-16 initially found `OpenProject` itself failing on the exact light-corruption fixture `internal/db/repair_selfheal_test.go`'s `corruptLightly` uses (byte offset 4097), raising the concern that `RepairAndSwap` might be unreachable exactly when needed. Resolved via a systematic page-by-page sweep (one byte flipped per 4096-byte page across a 105-page encrypted fixture, `OpenProject`/a real query/`RepairAndSwap` outcome recorded per page, byte restored before the next trial — reproducible, confirmed identical across repeated runs): `OpenProject` succeeded after single-page corruption on 80/105 pages (76%) — the earlier offset-4097 failure was one unlucky page (page 1, hit by `Migrate`'s schema/settings bootstrap), not evidence of a systemic problem. The sweep varied exactly one dimension — a single byte flipped in one page at a time — and did not cover multi-page corruption, torn/partial writes, or truncation, so this closure is scoped to single-page corruption specifically, not corruption in general. **Within that scope, `RepairAndSwap` is reachable in the large majority of cases** — `TestRepairAndSwapHealsReachableLightCorruption` (`repair_and_swap_test.go`) pins one such case end-to-end: `OpenProject` succeeds, a real query succeeds, then `RepairAndSwap` still detects the underlying corruption during its own full-database `VACUUM INTO` pass and heals it. **Residual finding, not a defect:** of the 3 pages (of 105) where corruption was severe enough that a real query failed even after `OpenProject` succeeded — genuinely user-visible corruption, exactly `RepairAndSwap`'s target scenario — `RepairAndSwap`'s own healing attempt also failed on all 3 in this sample, rather than producing a healthy copy. `TestRepairAndSwapCanFailToHealEvenWhenReached` pins this as current, real behavior (not asserted as a bug — no test can require `VACUUM INTO` to heal arbitrary corruption). This mirrors the same light-vs-severe corruption distinction `internal/db/repair_selfheal_test.go` already documents for plaintext SQLite (`corruptLightly` heals, `corruptSeverely` doesn't); this sweep confirms the same distinction holds on the encrypted path, with a small (n=3) sample suggesting user-visible corruption skews toward the unhealable side more than the overall 76%/24% split would suggest — worth a larger sample in a future cycle if this becomes a real support-burden signal, but not blocking: no code behavior change is needed to close this item, since `RepairAndSwap` was already doing the right thing when reachable. |

## P1: release trust and usability

| Item | Status | Completion evidence |
| --- | --- | --- |
| Sign and notarize distributables where supported | Blocked | Release credentials are configured and the published artifacts pass the platform trust checks. |
| Configure the signed update channel | In progress | A controlled prerelease-to-prerelease update verifies signature, channel, version, URL, and digest while offline use remains unaffected. |
| Obtain trusted PAdES evidence | Blocked | A release-certificate sample validates as trusted and independent interoperability evidence is archived. |
| Complete keyboard and assistive-technology smoke coverage | Planned | First run, project editing, dialogs, reports, and native window controls are exercised with the supported assistive tools. |
| Replace remaining `window.confirm()` dialogs with the app's own modal | Done — all known `confirm()` call sites in the app now route through a real DOM modal | Root cause confirmed 2026-08-17 by reading the vendored Wails source this repo pins (`github.com/wailsapp/wails/v2 v2.13.0`): `internal/frontend/desktop/darwin/WailsContext.m`'s `WailsContext` class declares conformance to `WKUIDelegate` but implements none of the `runJavaScript{Alert,Confirm,TextInput}Panel...` methods (`grep -rn runJavaScript` across the whole vendored module returns nothing) — no delegate method exists to present a panel. The observed result in a packaged macOS build (2026-08-16 GUI evidence) is that `window.confirm()`/`alert()`/`prompt()` produce no visible dialog and the calling code proceeds as if it got a falsy/no-op result, rather than throwing or falling back to a default; the exact value WebKit's `confirm()` returns in that case was not independently confirmed against WebKit source or documentation, only the missing-delegate cause and the no-dialog symptom. This was first surfaced as a flagged, unconfirmed side observation in the 2026-08-16 native-close GUI evidence (`.agent_memory/packaged-app-lifecycle-evidence-2026-08-16.md`, Stage C): `StakeholderManager`'s in-form Cancel/×/Escape silently no-op-ed on a dirty draft (an adjacent Save button at the same coordinates worked fine in the same session, ruling out input-registration flakiness) — not a data-loss defect, since the shared native-close guard's dirty registration survived every failed dismissal attempt, but a stuck-UI usability defect. **First pass (2026-08-17):** `StakeholderManager.requestClose()`, `WorkItemEditor.requestClose()`, and `SprintList.requestClose()` (the three "discard unsaved changes" confirms) routed through the existing shared Save/Discard/Cancel guard (`requestNavigation`/`navigation.pending` in `session.svelte.ts`) the native-close guard already uses. **Second pass (2026-08-17, same day):** the six remaining plain yes/no `confirm()` call sites — `StakeholderManager.destroy()` (delete stakeholder), `WorkItemEditor.destroy()` (delete work item), `SprintList.complete()` and `SprintList.destroy()` (complete/delete sprint), `DORADashboard.deleteDeployment()`, and `FishboneEditor.applySixMs()` (replace categories preset) — were all silently inert buttons in a packaged macOS build (clicking did nothing, no error, no visible dialog). Fixed by extracting the first genuine shared UI primitive beyond `Spinner.svelte`: `frontend/src/lib/components/ConfirmDialog.svelte`, a `role="alertdialog"` modal matching the existing guard's ARIA/structural pattern (`open`/`title`/`message`/`confirmLabel`/`cancelLabel`/`tone: 'danger'|'caution'`/`busy`/`onConfirm`/`onCancel`), wired into all six call sites. `SprintList.complete()` uses `tone="caution"` rather than the danger styling used everywhere else: completing a sprint has no reopen path anywhere in the app (no `ReopenSprint`/`UncompleteSprint` binding, and the row's own status-gated action button only shows for `'planning'`/`'active'`), so it's irreversible in effect though not a delete — verified by grepping the whole repo for a reopen binding before deciding the tone, not assumed. `StakeholderManager.destroy()`'s pre-existing `editing || busy` guard (which stops a background-row delete from firing while another draft is open or a save is in flight) was preserved unchanged, ahead of where the dialog is now opened — confirmed by the existing "does not replace or delete an open draft through background actions" test still passing unmodified. Regression-tested per site with a spy asserting `window.confirm` is never called on the guarded action, plus `ConfirmDialog.test.ts` itself proving Escape/Cancel/Confirm behavior is independent of `window.confirm`'s return value (mocked to throw). `DORADashboard.test.ts` and `FishboneEditor.test.ts` had zero prior coverage of any kind — both are new files, scoped narrowly to the delete/replace-preset path fixed here, not full-component coverage. All of this is source-level/jsdom evidence plus one live spot-check (create a stakeholder, delete it through the new dialog end-to-end in a running `wails dev` instance, confirm the row disappears) — not re-driven through a signed/packaged macOS build this cycle. |
| Fix launchpad wizard promising the Software-Dev Pack but leaving it disabled | Done — see `TestCreateProjectFromLaunchpadEnablesAgilePackWhenSeeded`/`TestCreateProjectFromLaunchpadLeavesAgilePackDisabledWithoutAgileSeeds` (`encryption_project_test.go`) | Found 2026-08-17 during a UX audit, then root-caused and fixed the same day: the New Project wizard's "Starter Artifacts" step defaults `kanban`/`backlog`/`sprint1` to checked for Scrum-ish methodologies (`internal/templates/launchpad_seeds.json` via `LaunchpadEvaluate`), which reads as an explicit promise that the Software-Dev Pack will be active on creation — the checklist literally says "Kanban board", "3 placeholder backlog items", "Sprint 1 in planning state". `internal/templates/seeds.go`'s `Seeder.Apply` seeded those rows into the project correctly, but never touched the separate `Settings.AgileEnabled` flag `Dashboard.svelte` gates the whole pack's visibility on (defaults `false`) — so a Scrum user landed on a project with real seeded data sitting behind a section labeled "SOFTWARE-DEV PACK (DISABLED)", discoverable only via a manual "Enable Agile Pack" button after scrolling past the entire chart/document tool catalog. Fixed in `CreateProjectFromLaunchpad` (`app_foundation.go`): after `seeder.Apply(seeds)`, if `seedErr == nil` (seeding completed exactly as promised — `Apply` short-circuits on its first error, so a partial failure must not silently enable a pack around artifacts that didn't all land) and the requested seeds include any of `kanban`/`backlog`/`sprint1`, load settings, set `AgileEnabled = true`, save settings — using the freshly-created project's still-open `*db.Database` directly, since `OpenProject` (which installs `a.db`) hasn't run yet at that point in the call. A settings-read or -write failure here folds into the existing `seedErr` bubble-up path (`"project created but seeding partial: %w"`) rather than being swallowed, matching how a seeding failure was already surfaced — an adversarial review caught the original version silently discarding both error returns. Two Go tests cover it: seeding `kanban`/`backlog`/`sprint1`/`charter` together results in `AgileEnabled() == true` and all four requested seeds have a receipt; seeding only `charter`/`wbs` (no agile artifacts) leaves it `false`. Live-verified end-to-end in a running `wails dev` instance with an isolated data root: created a project through the actual wizard with Scrum + all three starter-artifact boxes checked, and the resulting dashboard showed "SOFTWARE-DEV PACK" already enabled (button read "Disable") with Kanban/Backlog/Sprints/DORA Dashboard all visible immediately — not just a passing unit test. Post-implementation review note: the `seedErr == nil` guard's partial-failure branch — the pack must stay disabled whenever *any* requested seed fails, whether that's a different seed short-circuiting `Apply` before it reaches the agile one, or the agile seed itself failing — is logic-verified by reading `Seeder.Apply`'s short-circuit behavior, not exercised by either test above — both only cover the all-succeed and no-agile-seeds-requested cases. No test forces a seed failure to confirm the pack actually stays disabled in that case. |
| Extract a shared Button/Card/Input/Textarea/Select component library (design-critique Priority #2) | Foundational library done, 9 files touched as proof-of-concept (specific call sites only, not whole-file conversions); 68 of the app's 77 remaining screen files still contain at least one raw `<button>`/`<input>`/`<textarea>`/`<select>` element deliberately out of scope this cycle | A 2026-08-17 UX audit found the app had zero shared component library beyond `Spinner.svelte`/`ConfirmDialog.svelte`: ~217 hand-copied `<button>` class strings across 6+ divergent visual families (including 4 Sigma module files — `SigmaVoCCTQ`/`SigmaControlPlan`/`SigmaSolutionMatrix`/`SigmaSIPOC` — running an entirely separate, hardcoded-hex Bootstrap-style CSS button system that didn't respect the app's light/dark theme at all), ~307 `<input>`/`<textarea>`/`<select>` tags sharing one 90%-duplicated class string with inconsistent backgrounds and focus rings, and ~40 card/panel container variants including a confirmed byte-identical duplicate wrapper in `ChartCatalog.svelte`/`DocumentCatalog.svelte`. Built five real Svelte 5 components in `frontend/src/lib/components/` (`Button`, `Card`, `Input`, `Textarea`, `Select`) rather than a CSS-only utility class, using `$bindable()` plus a `...rest` prop spread so every native HTML attribute reaches the underlying element unchanged — this was identified pre-implementation as the main technical risk (wrapping ~307 call sites' worth of varied `type`/`min`/`max`/`list`/`aria-*` attributes) and resolved structurally rather than by attribute-by-attribute proxying. `Button`'s `variant`/`size` classes were derived from the real app-wide distribution, not an invented scale: `sm`/`md`/`lg` map to the three largest padding buckets found (`px-3 py-1`/`px-3 py-2`/`px-4 py-2`, 71% of sized buttons combined); the `link` variant (46 in-prose cross-reference occurrences across the whole Help subtree's 6 files, of which 23 live in `HelpFeatures.svelte` alone) is deliberately exempt from `size` and the shared base classes, since it has no padding/rounded/fixed-size in the original pattern and forcing it through the shared base would expand its inline box and disturb surrounding sentences; a `ghost` variant was added after a mid-implementation review caught that `ConfirmDialog`'s own Cancel button (transparent-at-rest, hover background) didn't match any variant in the first draft — two dedicated tests in `Button.test.ts` reconstruct `ConfirmDialog`'s original Cancel and danger-Confirm class strings token-for-token, proving the dogfooding migration needed zero `class` passthrough to preserve the appearance already live-verified in an earlier pass. `Card` is deliberately static-only (a `<div>`, no clickable/polymorphic mode) after a mid-implementation review flagged the app's card-*shaped* clickable tiles (Dashboard's stat/tool-catalog cards) as full custom rows whose behavior (text-left alignment, click handling) a generic polymorphic wrapper risked losing on the app's most-visited screen — the PoC instead targets the confirmed, lower-risk `ChartCatalog`/`DocumentCatalog` duplicate. `Input`/`Select` bake in `w-full mt-1` layout opinions that `class` passthrough cannot undo (Tailwind resolves conflicting utilities by stylesheet position, not class order) — a mid-implementation review caught this would have silently broken `StakeholderManager`'s inline header filter field, which was dropped from the migration scope rather than force-fit; `Select` also deliberately normalizes to one background and always includes the focus ring the app's selects usually lack, a consistency fix rather than a preservation, live-verified rather than assumed correct from passing tests. Adopted the literal `cyan-600`/`slate-800`/`red-700` palette rather than the `accent`/`critical` Tailwind config tokens after reading `--brand-accent`'s actual resolved value (`#00D4FF` dark, `#0E7490` light) and finding it matches neither theme's `cyan-600` (`#0891B2`) — adopting the token would have silently recolored every migrated button. Proof-of-concept migration, chosen for risk/value rather than exhaustiveness: all 4 Sigma files (11 of 12 `.btn`-prefixed button sites converted to `Button`; the 12th, `SigmaSIPOC`'s circular `.btn-remove` corner badge, is structurally unlike any `Button` variant and was left as local CSS with its hardcoded `#dc3545` fixed to the app's real red-700 in place instead), `ConfirmDialog.svelte` (dogfooding, see above), `StakeholderManager.svelte` (3 of 7 buttons that cleanly match a variant, all 8 modal form fields, and the Category select — the nav-back link, two icon-glyph buttons, and the full-row click target were deliberately left as documented, distinct patterns rather than force-fit), `ChartCatalog.svelte`/`DocumentCatalog.svelte` (the confirmed duplicate wrapper), and `HelpFeatures.svelte` (all 23 `link`-variant cross-reference buttons, mechanically verified 1:1 before and after with `grep -c`). Reaching the Sigma module's own screens to live-verify hit a genuine, unrelated, pre-existing backend bug — `SigmaCreateProject` generated a synthetic `sigma-<timestamp>` ID that never satisfied `sigma_projects`' `FOREIGN KEY (id) REFERENCES project(id)` constraint, so creating any DMAIC project failed outright — flagged separately rather than fixed inline at the time (see the "Fix Sigma DMAIC project creation" row below, same day, where it was fixed and live-verified); live visual verification of the 4 Sigma files at the time this row was written instead used a temporary, uncommitted Vite harness (`frontend/harness.html`/`harness.js`, deleted after use, never part of the repo) that mounted each component directly with a stubbed `window.go.main.App`, matching the existing vitest mocking convention. `StakeholderManager` was live-verified inside the real running app instead (create → save → delete a stakeholder end-to-end, including the `ConfirmDialog` delete flow), since it didn't depend on the broken Sigma feature. `make verify` (Go tests, `svelte-check --fail-on-warnings`, the full frontend vitest suite, and the production build) passes clean after every round. **Explicitly not done this cycle:** the remaining 68 files' worth of duplicated button/input/card patterns (measured post-implementation via `grep -rlE '<button|<input|<textarea|<select' src --include="*.svelte"`, excluding the library components' own markup and test harnesses; documented in the pre-implementation inventory but not migrated), and Priority #3 (Project Dashboard information-architecture restructuring) from the same design critique — both would need their own planning cycle, not a rushed sweep with insufficient regression-test coverage to safely verify no visual regressions across every editor screen. |
| Reconcile macOS package version-string sourcing | Done for macOS (darwin); Linux deliberately not applied | Spotted 2026-08-16 while inspecting build artifacts: `scripts/package-macos.sh` and `scripts/package-macos-installer.sh` derived their filename version differently. Root cause, found on investigation, was sharper than a mismatch: both scripts' original fallback used `git describe --tags --abbrev=0`, which silently reuses the nearest tag's bare name for a checkout that is any number of commits past it — two different trees could produce identically-named local-build artifacts. Fixed by extracting the derivation into `scripts/package-version-lib.sh` (`release_version()`), used by both scripts: explicit `VERSION` env var wins (this is what CI always sets — `release.yml:208,214` — so the real release pipeline was never exposed to this bug), else `git describe --tags` *without* `--abbrev=0` so a build past its tag gets an honest `-<N>-g<sha>` suffix, else `0.0.0`. Verified by rebuilding both artifacts at the same commit: filenames now agree exactly (`...1.1.0-alpha.1-132-g9a54f40...`), and the `.pkg`'s actual `PackageInfo` `version="1.1.0"` (the upgrade-comparison key, read from the app bundle's own Info.plist, unrelated to the filename) is unchanged. `scripts/package-version-lib_test.sh` proves the fix (case 3 fails if `--abbrev=0` is reintroduced) and is wired into `make verify` and both `make package-macos*` targets. Also discovered `scripts/package-macos_test.sh` itself was invoked by nothing — not the Makefile, not CI, not `check-release.sh` — and wired it into `package-macos:` alongside the fix. **`scripts/package-linux.sh` carries the identical `--abbrev=0` one-liner and is deliberately left unfixed.** 2026-08-16 (later cycle): the "no Linux host to verify nfpm" reasoning is now partly outdated — nfpm is a cross-platform Go binary, so its version-string handling doesn't need one. Installed the pinned `nfpm@v2.47.0` locally (`go install`, matches `scripts/release-tool-versions.env`) and ran both packagers against `VERSION=1.1.0-alpha.1-133-g26e4e69` in an isolated scratch config: **both succeed, exit 0, no error.** rpm `Version` → `1.1.0~alpha.1_133_g26e4e69` (first `-` → `~`, rest → `_`); deb `Version` → `1.1.0~alpha.1-133-g26e4e69` (first `-` → `~`, rest kept — legal in Debian). nfpm sanitizing at all confirms the original concern was real (raw hyphens aren't accepted); what this shows is that nfpm absorbs it rather than failing. **Still unverified, and now the actual blocker:** whether `rpm`/`dnf` itself accepts the sanitized Version and orders it correctly relative to a plain `1.1.0` release — no `rpm` binary and no Linux host reachable this cycle to check. Applying the same `release_version()` fix to `package-linux.sh` on this evidence alone was considered and declined: CI sets `VERSION` explicitly for this script too (`release.yml`), so the bug this would fix is local-dev-only, and `package-linux.sh` can't even run on this host (`build/bin/gopmgr` for linux/amd64 doesn't exist here) — the fix should land in a cycle with a real Linux host to verify install/upgrade ordering end to end, not be applied blind a second time just because the evidence looked tidy. |
| Close residual risks left by the Sigma DMAIC FK fix | Done | The Sigma DMAIC FK fix (row above) closed with three residual risks disclosed rather than silently left open. This cycle addressed all three. **Risk 1 (`repair_and_swap_test.go`'s hardcoded corruption offsets are coupled to fixture page layout)**: accepted as documented, no code change — the existing doc comment on `seedRepairFixtureProject` already carries the full re-derivation methodology and has now been used successfully twice (the original sweep, and the 2026-08-17 re-derivation after this same FK fix shifted the fixture's page layout); a scripted re-sweep runbook was considered and rejected as speculative tooling for a low-probability event with an already-working manual procedure. **Risk 2 (`SigmaListProjects` returned every `sigma_projects` row unfiltered by `project_id`)**: the list path is fixed. See `sigma_crud_test.go`'s ledger row for the full account, including a first attempt (a schema-level trigger) that was empirically validated in isolation, then reverted after a full `go test ./...` run showed it broke ten pre-existing tests that deliberately exercise multiple `project` rows per database — evidence that this codebase's multi-project-per-file future is closer than the schema comment's "shaped to support... later" suggested. The shipped fix threads the currently-open GoPMgr project's ID through `SigmaListProjects`/`ProjectService.ListProjects`/`App.SigmaListProjects`, adding `WHERE project_id = ?` to match the scoping pattern `ListDocuments`/`ListAuditEvents` already use elsewhere in this package. A mid-implementation adversarial review caught that this narrower framing ("the list is scoped") understated the original risk: `SigmaProjectView.svelte` fetches a specific Sigma project by id directly (`SigmaGetProject`), bypassing the list entirely, and that getter had no equivalent check. Fixed the same day by scoping `SigmaGetProject` too, which also acts as the load-bearing chokepoint for the six Charter/Fishbone/Solutions/ControlPlan/SIPOC/VoC sub-getters, since `loadProject()` calls it first and aborts on failure. The regression test written for that fix at the time (`TestSigmaGetProject_RejectsSigmaProjectFromAnotherFile`) was itself flawed — it used two separate `.gopmgr` files, which are independent SQLite databases, so the assertion passed on ordinary cross-database not-found regardless of whether the ownership check existed; this was caught and corrected in a later cycle (see `app_sigma_test.go`'s ledger row), which also closed the six sub-getters plus four more methods (`SigmaAdvancePhase`/`SigmaCheckReadiness`/`SigmaGetToolStatus`/`SigmaExportProjectReport`) that had the same gap — all 17 methods taking a bare Sigma project id are now scoped and verified against a same-file, multi-`project`-row fixture, with fault-seeding confirming the guard is load-bearing. **Risk 3 (sub-tab UI render under a Sigma-project switch was verified only at the save layer, not confirmed to re-render correctly)**: closed by static code trace, not a live run — `SigmaProjectView.svelte`'s only exit is `goto('sigma_dashboard')` (two call sites), and `App.svelte`'s route `$effect` depends only on `session.view`, so every path back into a *different* Sigma project necessarily passes through a `session.view` change that tears down and remounts `SigmaProjectView` (re-running its `onMount` against the new `session.editingId`) rather than reusing a stale instance; its child tabs (`SigmaSIPOC`, `SigmaVoCCTQ`, `SigmaSolutionMatrix`, `SigmaControlPlan`, `SigmaFishbone`) all take `projectId`/`project_id` as props from that freshly-mounted parent, not from any module-level or session-read state of their own. No direct sigma-project-to-sigma-project navigation path exists in the current UI to create the stale-render scenario the original risk described. |
| Close the remaining Sigma cross-tenant scoping gap; Priority #3 Penpot board | Done | Continuation of the row above's Risk 2 closure, picked as this cycle's "best next work": bounded, high value-to-risk, extends already-reviewed work rather than opening new scope. **Found the previous cycle's regression test was vacuous** (see `app_sigma_test.go`'s ledger row for the full account) — `TestSigmaGetProject_RejectsSigmaProjectFromAnotherFile` used two separate `.gopmgr` files, which are independent SQLite databases (`CreateProject` calls `db.InitEncryptedDB` on a fresh path every time), so the id from one file's Sigma project could never collide with another file's regardless of whether the ownership check worked. Replaced with a same-file, two-`project`-row fixture (seeded via raw SQL, matching the pattern this package's own `agile`/`documents`/`audit_events`/`scenarios` tests already use for `project_id`-scoping coverage) that actually exercises the guard, then fault-seeded it to confirm all assertions are load-bearing. Extended the fix itself from the 2 originally-scoped methods to all 17 App methods that take a bare Sigma project id — a mid-implementation adversarial review flagged that the prior cycle's disclosed "6 sub-getters" framing missed `SigmaAdvancePhase` (a write to `sigma_projects` itself), `SigmaCheckReadiness`/`SigmaGetToolStatus` (aggregating reads), and `SigmaExportProjectReport` (the widest read — exfiltrates a whole foreign project's report to disk). Refactored to one shared `requireSigmaProjectInOpenFile` helper in `app_sigma.go` rather than 17 inline checks, replacing `SigmaGetProject`'s own prior inline version, so there is exactly one implementation of the rule. Coverage cross-checked by grepping the full `Sigma*` method list against helper call sites: 17 guarded, 5 correctly excluded (`SigmaCreateProject`/`SigmaListProjects` don't take a bare Sigma id; the three `SigmaCalculate*` stats functions take no project id at all). Built the Priority #3 Dashboard IA proposal board in Penpot (`Dashboard-IA-Proposal-Board`, in the existing "GoPMgr UX/UI Redesign" project, positioned below `IA-Flow-Board` on the same page) — deferred across the two prior cycles because the Penpot MCP plugin had no live browser session; connected this cycle and built while the connection held, per explicit adversarial-review guidance not to defer it a third time. The board wireframes the design doc's proposed tabbed restructuring (meta-card strip, 4-tab row, a representative Overview-tab content panel, and a fix-summary callout), matching the existing boards' established color/typography conventions (read from `Dashboard.svelte`'s and `Card example`'s existing shapes rather than invented) — verified visually via `export_shape` before being called done, catching one real issue in the process (a `→` arrow character rendered as a near-blank glyph in the `sourcesanspro` font used throughout the project; replaced with `->` after `←` was confirmed to render correctly, so only the one specific glyph needed the fix, not every arrow in the board). `make verify` passes clean; no Go method signatures changed (all 17 guarded methods kept their existing parameters, only new possible error returns for a foreign/invalid id), so no frontend or Wails-binding changes were needed. |
| Priority #3 Dashboard IA restructuring — R1/R2 (naming collision, chart-fallback view) | R1/R2 done; R3/R4 (the tabbed restructuring itself) deliberately deferred to its own cycle | First two of the four risks (`docs/design/dashboard-ia-restructuring-proposal.md`) closed this cycle; R3/R4 scoped out with an explicit reason rather than silently dropped (see below). **R1 (portfolio/dashboard naming collision)**: three separate screens informally shared the word "dashboard" — `AppHeader`'s nav labeled the `portfolio` view "Dashboard", `Portfolio.svelte`'s own `<h1>` read "Portfolio dashboard", and a wholly separate per-project `dashboard` view existed alongside both. Fixed by relabeling the nav item and the heading to "Portfolio" (`AppHeader.svelte`, `Portfolio.svelte`), plus the two `HelpOverview.svelte`/`HelpReference.svelte` prose references that echoed the old name. Regression coverage: `AppHeader.test.ts` (nav label, `aria-current`, admin-item gating) and `Portfolio.test.ts` (heading text) — 4 new tests. **R2 (`'charts'` registry-drift fallback)**: `session.view === 'charts'` is a real, already-tested-as-reachable state (`Dashboard.test.ts`'s existing "falls back to the generic charts view" test) hit when `Dashboard.svelte` saves a chart whose `kind` has no entry in the frontend's `chartRoutes` map — a registry drift between `internal/charts/registry.go` and the frontend, previously falling through to the app's generic "Unknown view" screen with no explanation. Built `ChartFallback.svelte`: looks up the saved chart by id via `GetChart` and names it in the confirmation copy on success, falls back to generic "your chart was saved" text plus a disclosure note on lookup failure — deliberately phrased as success, not error, since both call sites that reach this view only navigate here after `SaveChart` has already succeeded. 3 new tests in `ChartFallback.test.ts`. **Design doc self-correction**: re-reading `Dashboard.svelte` during implementation (not just trusting the doc's own prior claims) found and fixed 4 factual errors in the doc: a claimed visible "Project dashboard" heading that's actually only a screen-reader-only label; a "five kinds of content" count that was actually seven (meta cards, Sigma quick-link, charts list, documents list — previously wrongly merged into one, chart catalog, document-template catalog, Software-Dev Pack); a "the Documents list" (singular) reference in the proposed Overview tab that's actually two separate lists; and a proposed Dev Tools tab gated on `session.project.methodology`, when the Software-Dev Pack toggle is actually unconditional/universal in the real code. All four corrected with explicit "Correction" language in the doc, not silent edits. Penpot `Dashboard-IA-Proposal-Board` rebuilt to match the corrected doc (implementation-status subtitle, DMAIC quick-link row, split Overview-tab charts/documents content, corrected unconditional Dev Tools tab label with a full-width footnote explaining the universal toggle) — one clipping bug found and fixed in the process (a footnote positioned under a single tab column got cut off at the board edge in export; repositioned full-width below the tab row). **R3/R4 (the actual tabbed restructuring of `Dashboard.svelte`) deliberately not implemented this cycle** — reason recorded in the design doc's §7: `Dashboard.test.ts` has 23 tests across 8 describe blocks, most touching content that would move under tab-switch logic; Testing Library excludes non-visible elements, so those assertions would need rewriting alongside the restructuring, not after it; and the component has a documented pre-existing load-order race (`load()`'s five sequential backend calls assigning state as each resolves while interactive controls render immediately, unguarded by a `loading` gate except where already fixed for the Agile toggle) that would make mixing a DOM restructuring with a test rewrite risk ambiguous failure attribution if something broke. Not live-verified in a running Wails build this cycle — the plain-vite dev server has no Go bindings (`window.go` is `undefined`, confirmed empirically); verified by `vitest run` (223/223 passing) and `svelte-check --fail-on-warnings` (0 errors/warnings) plus `make verify`'s full gate, all clean. |
| Priority #3 Dashboard IA restructuring — R3/R4 (tabbed restructuring) | Done | Continuation of the row above, its own cycle as planned: `docs/design/dashboard-ia-restructuring-proposal.md` §3.3/§7 now marks R3/R4 implemented. `project/Dashboard.svelte`'s seven content groups (§1's corrected count) restructured into a header-level meta-card strip (always visible, unchanged) plus a four-tab row (Overview default, Charts, Documents, Dev Tools) built on a new shared `Tabs.svelte` component (`role="tablist"/"tab"`, roving tabindex, arrow-key + Home/End navigation — the W3C ARIA APG pattern, C3). Measured rather than estimated the pre-existing test suite's actual edit surface by running it unmodified against the restructured component first: 12 of 23 tests failed (chart creation, document creation, Agile Pack toggle, MSPDI import — the content that moved off the default tab), 11 passed unchanged (load, chart/document deletion, signed export, project close — reachable from Overview or the always-visible header). Each of the 12 got one `switchTab()` call inserted at its point of interaction; the toggle-vs-load() race test was fault-seeded after its edit (guard temporarily removed, confirmed the test still catches the regression) to prove the insertion didn't make it vacuous. Three implementation decisions surfaced only while building, all recorded in the design doc as explicit corrections rather than silent deviations: the loading/error indicator stays outside all four tab panels (visible regardless of active tab, since the Charts/Documents catalogs depend on the same load) rather than nested in Overview as literally described; panels are `{#if}`-gated rather than kept mounted with `hidden` (the alternative was pre-implementation-reviewed and rejected — with all panels mounted, a chart kind's name can exist twice in the DOM at once, breaking a pre-existing `getByText` assertion in the load test); and a new one-line "nothing created yet" empty-state message on the Overview tab, needed because an empty project's Overview tab would otherwise render nothing at all. 13 new tests (`Tabs.test.ts` ×8 for the shared component's keyboard mechanics, `Dashboard.test.ts` ×5 for its own tab wiring only — including a check that a `session.project.methodology` test override actually took effect, not just that Dev Tools happens to always render). Not live-verified in a running Wails build — same disclosed constraint as R1/R2 last cycle, but higher-stakes here since this changed the DOM structure of the app's most-visited screen rather than just strings; C3 (keyboard/focus quality) is verified by assertion (`tabindex`, `aria-selected`, `document.activeElement`) only, not by a real focus ring in a real window. `make verify` passes clean. This closes Priority #3's tab-restructuring scope in full; the `ChartCatalog`/`DocumentCatalog` search/filter-state-reset item noted as a possible follow-up here was fixed 2026-08-18 — see the row below. |
| Live-verify R3/R4 in a real Wails build; fix catalog filter-state reset; close the `Button` icon-variant gap | Done | 2026-08-18, continuing directly from the two rows above. **Live verification**: launched `wails dev` (real Go bindings via `window.go`, confirmed by the `Using DevServer URL: http://localhost:34115` log line, not the plain-vite server used by earlier cycles which has none) with an isolated `XDG_DATA_HOME`, and drove it through the browser pane — signed in, created a project, exercised all four Dashboard tabs, chart/document creation and deletion, and the Agile Pack toggle — zero defects in the previously-jsdom-only R3/R4 work. A later `wails dev` restart in this same cycle, done to capture supplementary screenshot evidence for Penpot, hit an environment guardrail blocking automated credential entry (sign-in/account-creation is treated as a prohibited automated action); recovered by finding a browser tab still authenticated from before the restart (the isolated data directory was reused, so the session persisted) — its project (`testguy`, a Pie Chart) was created by the repository owner directly, not by automation, and supplied the real screenshot sent to the user and the Penpot callout's supporting evidence. Diagnosed and ruled out an apparent regression: ~46 "Canvas is already in use" console errors appeared after extended interactive testing; a clean page reload plus a fresh chart creation produced none, confirming this was accumulated HMR/rapid-testing churn in this session, not a defect in the untouched `BarEditor.svelte` Chart.js logic. Also root-caused and worked around a browser-pane tooling issue: coordinate-based clicks were not reliably registering on this dev-server page (a viewport/screenshot scaling mismatch specific to this environment); switched to `javascript_tool`-driven `element.click()` calls for all further interaction, which exercises the real Svelte handlers but not the physical pointer-event/hit-target path — disclosed as a methodology limitation, not silently worked around. **Catalog filter-state fix**: `ChartCatalog.svelte`/`DocumentCatalog.svelte`'s `expanded`/`query`/`engine`(`phase`) became `$bindable()` props, bound by `Dashboard.svelte` (`bind:expanded`/`bind:query`/`bind:engine`/`bind:phase`) so the state survives the `{#if}`-gated tab panels' remount on tab switch, instead of resetting every time as the R3/R4 design doc had disclosed and deferred. One fault-seeded regression test proves it. See `docs/design/dashboard-ia-restructuring-proposal.md` §7 and `TEST_COVERAGE_LEDGER.md` for full detail, including the `initiallyExpanded` prop's now-test-only role. **`Button` icon-variant gap**: `Button.svelte` previously had no `...rest` prop spread, a gap explicitly flagged in this file's Priority #2 row as blocking the app's 21 `aria-label="Remove …"` icon-glyph buttons from migrating. Added `variant="remove"` (`text-slate-500 hover:text-red-400`, no size/padding/rounding — matching the existing icon-glyph footprint) and `...rest` spread on all three non-default variant branches. Migrated all 21 sites across 14 chart/document-editor files (inventory and per-file counts in `TEST_COVERAGE_LEDGER.md`'s `Button.test.ts` row); `RACIEditor`'s one non-`×`-glyph outlier ("Remove role") required converting its `aria-label`/`title` from Svelte's in-quote template syntax to a JS template literal, since component props (unlike native attributes) are plain JS expressions. 4 new `Button.test.ts` tests plus `svelte-check` (0 errors) and 2 of the 21 sites live-verified directly in the running app (`BarEditor` before the bulk migration, `PieEditor` after) are the regression guard for this migration — the other 19 sites were not individually live-verified, since they render the same shared component code path. `make verify` (Go tests, `svelte-check --fail-on-warnings`, full frontend vitest suite, production build) passes clean; the pre-existing `makensis` crash in `windows-installer-scaffold` (a known Homebrew/NSIS host defect, unrelated to this change) was bypassed with `GOPMGR_SKIP_NSIS_COMPILE=1`, which is a skip, not a pass, and template syntax was not re-verified by `makensis` itself this cycle. **Explicitly not done this cycle**: Priority #2's remaining ~68-file shared-component migration surface, and Project Settings' flat-form-to-tabs restructuring (though `Tabs.svelte` is now available as a second consumer — done 2026-08-18, see the row below). |
| Fix Sigma DMAIC project creation (FK constraint made the module unusable) | Done | Flagged 2026-08-17 during the Priority #2 component-library work (see that row above): `SigmaCreateProject` generated a synthetic `sigma-<timestamp>` ID that never satisfied `sigma_projects.id`'s `FOREIGN KEY (id) REFERENCES project(id)` constraint, so every DMAIC project creation failed outright — the entire Six Sigma module (`SigmaWorkspace`/`SigmaProjectView` and all its sub-tabs) was unreachable end-to-end. The user's own initial diagnosis (`sigma_projects.id` forced equal to the open GoPMgr project's own ID, fixed by having `CreateProject` reuse it) was investigated and found **wrong**, not merely incomplete: the same schema forces at most one `sigma_projects` row per file, but `SigmaWorkspace.svelte`'s real UI is a card grid that creates and lists *several* DMAIC projects per file, and `SigmaProjectView.svelte` navigates by an arbitrary Sigma project ID (`session.editingId`), not a fixed one. Reusing the GoPMgr project's ID would have fixed the first create and then failed every subsequent one on a primary-key collision — confirmed as the wrong fix via a stronger-model advisor consult before writing any code, not assumed. Root-caused instead by comparing `sigma_projects`' schema against every other "many rows belong to one project file" table in `internal/db/sqlite.go` (`charts`, `baselines`, `scenarios`): all of them use an independently-generated `id` plus a separate `project_id` column referencing `project(id)` — `sigma_projects` was the sole outlier, using its own PK as the FK. Fixed by giving `sigma_projects` that same shape (`project_id TEXT NOT NULL, FOREIGN KEY (project_id) REFERENCES project(id)`), adding `domain.Project.GopmgrProjectID` to carry it, generating `sigma_projects.id` via `newID("sigma")` (mirroring `SaveChart`/`UpsertProject`'s existing convention) instead of a timestamp, and resolving the open GoPMgr project's ID in `app_sigma.go`'s `SigmaCreateProject` (mirroring `app_charts.go`'s `SaveChart`, the established pattern for this exact kind of parent-ID backfill) rather than inventing a new one. SQLite can't `ALTER` a `PRIMARY KEY`/`FOREIGN KEY` in place, so existing files need a real migration (`migrateSigmaProjectsSchema` in `sqlite.go`): probes for the `project_id` column first (no-op if already present, including every brand-new file, whose static schema already has the corrected shape), and otherwise disables FK enforcement on a single reserved connection (`db.Conn.Conn`, not a bare pooled `Exec` — `PRAGMA foreign_keys` is per-connection state, and the pool could otherwise silently run the rebuild on a different connection than the one that disabled it, a hazard this same file's `withConnPragmas` comment already warns about for a different pragma), rebuilds the table (`CREATE new` → `INSERT ... SELECT ... JOIN project ON project.id = sigma_projects.id`, safe because the old FK-as-PK schema made that equality hold for every existing row by construction, with `ON DELETE CASCADE` ruling out an orphaned row — `DROP` → `RENAME`), re-enables FK enforcement, and verifies with `PRAGMA foreign_key_check`. A second, previously-undiscovered issue surfaced mid-fix: `EnsureProjectSigmaLink`, a function with its own ledgered test but **zero production callers** (not `app_sigma.go`, not the frontend), encoded the *opposite* 1:1 design and would have silently no-op'ed on every call under the corrected schema (empirically verified: SQLite's `INSERT OR IGNORE` swallows a `NOT NULL` violation rather than erroring) — asked the user directly rather than deciding unilaterally, given it meant deleting previously-tested code; the user chose deletion over inventing new semantics for a function nothing calls. Regression coverage requires **two** Sigma projects under one GoPMgr project to succeed with distinct IDs, both returned by `SigmaListProjects` (`TestSigmaCreateProject_MultipleProjectsPerFile`, `internal/db/sigma_crud_test.go`) — a weaker one-create-succeeds test would also have passed under the rejected fix — plus a dedicated migration test against a hand-built legacy-schema file, proving both data preservation and idempotency across repeated `InitDB` opens (`sigma_migration_test.go`, new file). Fixing the schema shifted an unrelated fixture's on-disk page layout enough to break `TestRepairAndSwapCanFailToHealEvenWhenReached`'s hardcoded corruption offset (page 99 landed on a page `Migrate()` now fails to open outright) — caught by a full `go test ./...` run rather than assumed unaffected, and re-derived to page 100 via the same page-sweep methodology that fixture's own doc comment prescribes for exactly this situation, confirmed reproducible across 5 runs (see `repair_and_swap_test.go`'s ledger row). Live-verified end-to-end in a real `wails dev` instance against an isolated scratch account (`XDG_DATA_HOME` override, not the developer's real data) rather than through unit tests alone: created two DMAIC projects (one via the real `SigmaCreateProject` Go binding, one by filling out and submitting the actual "Start New DMAIC Project" form in the browser), confirmed both render as separate cards in the workspace grid, and confirmed navigating into each independently loads its own Define-phase tab set (Project Charter/SIPOC/VoC) correctly — this also closes the residual risk flagged in the Priority #2 backlog row above, where the 4 migrated Sigma `.svelte` files could only be visually verified through a stubbed harness because this exact bug blocked the real app. `make verify` passes clean (Go tests, `svelte-check`, frontend vitest, production build). |

| Project Settings tab restructuring; fix Methodology freeform-input inconsistency | Done | 2026-08-18 (`docs/design/project-settings-tab-restructuring.md`), continuing from the R3/R4 rows above: two findings from the same original design-critique pass, both on `project/ProjectSettings.svelte`, neither fixed until now. **Tab restructuring**: the screen's ~2,000-line, 10-section flat scroll became 5 tabs on the shared `Tabs.svelte` — its first validation as a second consumer, confirming the component generalizes beyond `Dashboard.svelte`. General (Identity/Classification/Lifecycle, bound to `draft`/`save()`/`revert()`, default tab), Scenarios, Resources, Exports & Signing (Schedule Reports + Export & Signature Settings + Document Font), Data Protection (Project Backup + Database Encryption — deliberately isolated rather than folded into Exports & Signing, so the two highest-stakes actions on this screen, encrypting the database and creating a backup, stay easy to find). A first attempt at physically reordering two sections during the refactor left the file with duplicate DOM ids and a broken `aria-controls` target — caught and fixed before any test or verification pass, not shipped and found later. A pre-implementation review forced a decision on `revert()`'s scope (it discards every `draft` field, all on the General tab): kept the header Save/Revert buttons global rather than adding new per-tab UI, but relabeled them "Revert details"/"Save details" so their scope survives a tab switch. Measured the pre-existing 2-test suite's actual edit surface by running it unmodified first (both failed, both live in the section that moved to a non-default tab) and inserted exactly the tab-switches that measurement showed were needed. **Methodology field**: was a bare freeform `<input>`, inconsistent with `ProjectLaunchpad.svelte`'s industry-scoped 3-card picker used at project creation — the same design-critique finding that flagged the flat form also flagged this. Replaced with a `<select>`; `db.Project.Methodology` has no server-side whitelist (confirmed by reading `app_foundation.go`'s `UpdateProjectIndustry`, not assumed), so an out-of-list current value is prepended as its own `"<value> (current)"` option rather than silently dropped or changed by merely opening the select — the whole point of this fix was consistency, not a chance to lose data. `ProjectLaunchpad.svelte`'s own `METHODOLOGIES` table was extracted to a new shared `frontend/src/lib/methodologies.ts` so the two screens can no longer independently drift on what a given industry's methodology options are, closing the underlying cause at the data level. Mirrors the wizard's own behavior of clearing methodology when industry changes. 9 tests total (2 pre-existing PAdES tests, 3 new tab-wiring tests, 4 new methodology tests) — one fault-seeded (a defensive `?? ''` load-time coercion, guarding a mock/future-field-removal case since `internal/db/project.go:39` confirms a real `GetProjectMeta` response can't actually omit the field). Live-verified end-to-end in a running `wails dev` build: created a project through the actual wizard (Software/Web Dev/Scrum), confirmed all 5 tabs render correct, isolated content (checked via `aria-selected`/`aria-controls`/panel-id inspection after an initial screenshot-timing artifact looked like a tab/content mismatch and turned out not to be one), confirmed the Methodology field renders as a real dropdown showing "Scrum", confirmed changing Industry to Construction clears it and enables Save/Revert, confirmed Revert restores both fields together and re-disables both buttons, and confirmed a Scenario-name draft survives a Scenarios → Resources → Scenarios switch. No new console errors beyond the single pre-existing Wails dev-server IPC quirk documented in earlier cycles. `make verify` passes clean. **Not done this cycle**: the Penpot board update — the plugin's browser-session connection had dropped by the time of this pass and could not be re-established without the user reconnecting it; disclosed rather than silently skipped, and closed the row below. Priority #2's remaining shared-component migration surface remains largely untouched — see the row below for the slice closed next. |
| Penpot board update for the row above; close 5 more Priority #2 remove-variant sites | Done | 2026-08-18, same day, continuing from the row above once the Penpot plugin connection was re-established (it had dropped mid-previous-cycle; the user reconnected it this cycle). **Penpot**: built a new `ProjectSettings-Tab-Restructuring-Board` (same page as `Dashboard-IA-Proposal-Board`, matching its palette/typography/layout conventions exactly — header bar, 5-tab row with General active, a representative General-tab content panel showing the Methodology field's before/after, and two green implemented/live-verified callouts) and added a short continuation callout to the existing `Style-Guide-Board`, correcting two claims that had gone stale there: the "Button has no `...rest` spread yet" gap note (closed two cycles ago) and the proof-of-concept file-count footer (never updated after the icon-variant migration). Both verified visually via `export_shape` before being called done. **Button migration continuation** (design-critique Priority #2): re-grepped for the exact `text-slate-500 hover:text-red-400` icon-glyph pattern (not just `aria-label="Remove …"`, the narrower search the prior icon-variant cycle used) and found it in `StakeholderManager.svelte` ("Delete stakeholder", previously named as a deliberate exclusion when `Button` had no icon-button variant — no longer applicable now that it does), `SprintList.svelte` ("Delete"), `DORADashboard.svelte` ("Delete deployment"), and `DocumentFieldEditor.svelte` (two sites: `removeStringRow`'s `×` and `removeObjectRow`'s "Remove row" — the first has a trailing `px-2` that made it miss the prior cycle's exact-string grep). All 5 sites across 4 files migrated to `Button variant="remove"`, preserving the extra `text-xs`/`text-xs px-2` each one carried via `class` passthrough. Every existing test for the 3 already-tested files (`StakeholderManager.test.ts`, `SprintList.test.ts`, `DORADashboard.test.ts`) passed unmodified (all query by `aria-label` or text content, not markup shape); one assertion was added per file pinning the migrated button's exact `className`, and each was fault-seeded by reverting its migration and confirming the assertion fails on the old string before restoring — proving the new assertions are load-bearing, not decorative. `DocumentFieldEditor.svelte` had zero prior test coverage; added a new, narrowly-scoped `DocumentFieldEditor.test.ts` (3 tests, covering only the two branches this migration touched) with the same fault-seed treatment. **Explicitly deferred, not silently dropped**: `WorkflowEditor.svelte` and `ActivityEditor.svelte` ("Delete edge", exact class match) and `CPMEditor.svelte` ("Remove assignment", `px-1` variant) share the identical pattern but have zero existing test coverage and non-trivial `onMount`/`session`/`autosave` state; a render-plus-one-click test would have proven the file imports, not that the migration is safe, so these were left unmigrated pending either a real test suite or live verification in a future cycle. **Explicitly excluded, distinct patterns**: `GanttEditor.svelte`'s two delete buttons use `text-slate-600` (not `-500`) — migrating would be a visible color change, not a preservation; `AdminPanel.svelte`'s red-hover link uses an underlined, different-sized text family entirely. **Recount, not estimate**: re-ran the exact inventory command (`grep -rlE '<button\|<input\|<textarea\|<select' src --include="*.svelte"`, excluding the library components and test harnesses) after all edits — file count is unchanged at 73 (was 73 before this row; the 68-file figure quoted in earlier rows was itself already stale by this point, see below), since all 4 touched files retain other raw elements outside this cycle's scope (e.g. `StakeholderManager`'s header filter `<select>`, `SprintList`'s Start/Complete buttons, `DocumentFieldEditor`'s other five field-type branches). This row closes 5 button sites, not files — reporting a file-count drop would overstate the result. `make verify` (Go tests, `svelte-check --fail-on-warnings`, full frontend vitest suite, production build, coverage-ledger-current check) passes clean. |

## P2: follow after beta stability

- Restore a usable normal window frame across relaunch.
- Improve local project discovery without weakening data isolation.
- Expand release validation only after it has an owner, target, and repeatable
  acceptance criteria.

## Maintenance rules

- Use `Planned`, `In progress`, `Blocked`, `Done`, or `Deferred`.
- A completed item must link to current tests or an archived release artifact;
  source inspection alone is insufficient for native behavior.
- Keep this file concise. Put durable architecture decisions in an ADR and
  point-in-time investigation details in Git history.
